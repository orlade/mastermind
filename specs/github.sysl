##########################################
##                                      ##
##  AUTOGENERATED CODE -- DO NOT EDIT!  ##
##                                      ##
##########################################

GitHub "GitHub v3 REST API":
    @version = "1.1.3"
    @termsOfService = "https://docs.github.com/articles/github-terms-of-service"
    @license = "MIT"
    @host = "api.github.com"
    @description =:
        | GitHub's v3 REST API.

    /issues:
        GET ?filter=string?&state=string?&labels=string?&sort=string?&direction=string?&since=string?&collab=bool?&orgs=bool?&owned=bool?&pulls=bool?&per_page=int?&page=int?:
            return error
            return error <: basic-error
            return error <: validation-error
            return ok <: _issues_ok

    /orgs/{org<:string}/issues:
        GET ?filter=string?&state=string?&labels=string?&sort=string?&direction=string?&since=string?&per_page=int?&page=int?:
            return error <: basic-error
            return ok <: _orgs__org__issues_ok

    /repos/{owner<:string}/{repo<:string}/issues:
        GET ?milestone=string?&state=string?&assignee=string?&creator=string?&mentioned=string?&labels=string?&sort=string?&direction=string?&since=string?&per_page=int?&page=int?:
            return error
            return error <: basic-error
            return error <: validation-error
            return ok <: _repos__owner___repo__issues_ok

    /repos/{owner<:string}/{repo<:string}/issues/comments:
        GET ?sort=string?&direction=string?&since=string?&per_page=int?&page=int?:
            return error <: basic-error
            return error <: validation-error
            return ok <: _repos__owner___repo__issues_comments_ok

    /repos/{owner<:string}/{repo<:string}/issues/comments/{comment_id<:int}:
        GET:
            return error <: basic-error
            return ok <: issue-comment

    /repos/{owner<:string}/{repo<:string}/issues/comments/{comment_id<:int}/reactions:
        GET ?content=string?&per_page=int?&page=int?:
            return error <: basic-error
            return error <: object
            return ok <: _repos__owner___repo__issues_comments__comment_id__reactions_ok

    /repos/{owner<:string}/{repo<:string}/issues/events:
        GET ?per_page=int?&page=int?:
            return error <: validation-error
            return ok <: _repos__owner___repo__issues_events_ok

    /repos/{owner<:string}/{repo<:string}/issues/events/{event_id<:int}:
        GET:
            return error <: basic-error
            return ok <: issue-event

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}:
        GET:
            return error
            return error <: basic-error
            return ok <: issue

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/comments:
        GET ?since=string?&per_page=int?&page=int?:
            return error <: basic-error
            return ok <: _repos__owner___repo__issues__issue_number__comments_ok

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/events:
        GET ?per_page=int?&page=int?:
            return error <: basic-error
            return ok <: _repos__owner___repo__issues__issue_number__events_ok

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/labels:
        GET ?per_page=int?&page=int?:
            return error <: basic-error
            return ok <: _repos__owner___repo__issues__issue_number__labels_ok

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/reactions:
        GET ?content=string?&per_page=int?&page=int?:
            return error <: basic-error
            return error <: object
            return ok <: _repos__owner___repo__issues__issue_number__reactions_ok

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/timeline:
        GET ?per_page=int?&page=int?:
            return error <: basic-error
            return error <: object
            return ok <: _repos__owner___repo__issues__issue_number__timeline_ok

    /repos/{owner<:string}/{repo<:string}/keys:
        GET ?per_page=int?&page=int?:
            return ok <: _repos__owner___repo__keys_ok

    /repos/{owner<:string}/{repo<:string}/keys/{key_id<:int}:
        GET:
            return error <: basic-error
            return ok <: deploy-key

    /repos/{owner<:string}/{repo<:string}/labels:
        GET ?per_page=int?&page=int?:
            return error <: basic-error
            return ok <: _repos__owner___repo__labels_ok

    /repos/{owner<:string}/{repo<:string}/labels/{name<:string}:
        GET:
            return error <: basic-error
            return ok <: label

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/labels:
        PUT (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: basic-error
            return error <: validation-error
            return ok <: sequence of label

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/lock:
        PUT (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: basic-error
            return error <: validation-error
            return ok

    /repos/{owner<:string}/{repo<:string}/issues:
        POST (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: basic-error
            return error <: object
            return error <: validation-error
            return ok <: _repos__owner___repo__issues_ok

    /repos/{owner<:string}/{repo<:string}/issues/comments/{comment_id<:int}/reactions:
        POST (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: object
            return error <: validation-error
            return ok <: reaction

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/assignees:
        POST (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return ok <: issue-simple

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/comments:
        POST (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: basic-error
            return error <: validation-error
            return ok <: _repos__owner___repo__issues__issue_number__comments_ok

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/labels:
        POST (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: basic-error
            return error <: validation-error
            return ok <: sequence of label

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/reactions:
        POST (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: object
            return error <: validation-error
            return ok <: reaction

    /repos/{owner<:string}/{repo<:string}/keys:
        POST (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: validation-error
            return ok <: _repos__owner___repo__keys_ok

    /repos/{owner<:string}/{repo<:string}/labels:
        POST (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: basic-error
            return error <: validation-error
            return ok <: _repos__owner___repo__labels_ok

    /repos/{owner<:string}/{repo<:string}/issues/comments/{comment_id<:int}:
        DELETE:
            return ok

    /repos/{owner<:string}/{repo<:string}/issues/comments/{comment_id<:int}/reactions/{reaction_id<:int}:
        DELETE:
            return ok

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/assignees:
        DELETE (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return ok <: issue-simple

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/labels:
        DELETE:
            return error <: basic-error
            return ok

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/labels/{name<:string}:
        DELETE:
            return error <: basic-error
            return ok <: sequence of label

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/lock:
        DELETE:
            return error <: basic-error
            return ok

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}/reactions/{reaction_id<:int}:
        DELETE:
            return ok

    /repos/{owner<:string}/{repo<:string}/keys/{key_id<:int}:
        DELETE:
            return ok

    /repos/{owner<:string}/{repo<:string}/labels/{name<:string}:
        DELETE:
            return ok

    /repos/{owner<:string}/{repo<:string}/issues/comments/{comment_id<:int}:
        PATCH (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error <: validation-error
            return ok <: issue-comment

    /repos/{owner<:string}/{repo<:string}/issues/{issue_number<:int}:
        PATCH (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return error
            return error <: basic-error
            return error <: object
            return error <: validation-error
            return ok <: issue

    /repos/{owner<:string}/{repo<:string}/labels/{name<:string}:
        PATCH (objectRequest <: objectRequest [mediatype="application/json", ~body]):
            return ok <: label

    #---------------------------------------------------------------------------
    # definitions

    !type _issues_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        issue <: sequence of issue [mediatype="application/json"]:
            @json_tag = "issue"

    !type _orgs__org__issues_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        issue <: sequence of issue [mediatype="application/json"]:
            @json_tag = "issue"

    !type _repos__owner___repo__issues__issue_number__comments_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        issue-comment <: sequence of issue-comment [mediatype="application/json"]:
            @json_tag = "issue-comment"

    !type _repos__owner___repo__issues__issue_number__comments_ok:
        Location <: string [~header]:
            @json_tag = "Location"
        issue-comment <: issue-comment [mediatype="application/json"]:
            @json_tag = "issue-comment"

    !type _repos__owner___repo__issues__issue_number__events_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        issue-event-for-issue <: sequence of issue-event-for-issue [mediatype="application/json"]:
            @json_tag = "issue-event-for-issue"

    !type _repos__owner___repo__issues__issue_number__labels_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        label <: sequence of label [mediatype="application/json"]:
            @json_tag = "label"

    !type _repos__owner___repo__issues__issue_number__reactions_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        reaction <: sequence of reaction [mediatype="application/json"]:
            @json_tag = "reaction"

    !type _repos__owner___repo__issues__issue_number__timeline_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        issue-event-for-issue <: sequence of issue-event-for-issue [mediatype="application/json"]:
            @json_tag = "issue-event-for-issue"

    !type _repos__owner___repo__issues_comments__comment_id__reactions_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        reaction <: sequence of reaction [mediatype="application/json"]:
            @json_tag = "reaction"

    !type _repos__owner___repo__issues_comments_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        issue-comment <: sequence of issue-comment [mediatype="application/json"]:
            @json_tag = "issue-comment"

    !type _repos__owner___repo__issues_events_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        issue-event <: sequence of issue-event [mediatype="application/json"]:
            @json_tag = "issue-event"

    !type _repos__owner___repo__issues_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        issue-simple <: sequence of issue-simple [mediatype="application/json"]:
            @json_tag = "issue-simple"

    !type _repos__owner___repo__issues_ok:
        Location <: string [~header]:
            @json_tag = "Location"
        issue <: issue [mediatype="application/json"]:
            @json_tag = "issue"

    !type _repos__owner___repo__keys_ok:
        Location <: string [~header]:
            @json_tag = "Location"
        deploy-key <: deploy-key [mediatype="application/json"]:
            @json_tag = "deploy-key"

    !type _repos__owner___repo__keys_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        deploy-key <: sequence of deploy-key [mediatype="application/json"]:
            @json_tag = "deploy-key"

    !type _repos__owner___repo__labels_ok:
        Link <: string [~header]:
            @json_tag = "Link"
        label <: sequence of label [mediatype="application/json"]:
            @json_tag = "label"

    !type _repos__owner___repo__labels_ok:
        Location <: string [~header]:
            @json_tag = "Location"
        label <: label [mediatype="application/json"]:
            @json_tag = "label"

    !type actions-billing-usage:
        included_minutes <: int?:
            @json_tag = "included_minutes"
        minutes_used_breakdown <: actions-billing-usage_minutes_used_breakdown?:
            @json_tag = "minutes_used_breakdown"
        total_minutes_used <: int?:
            @json_tag = "total_minutes_used"
        total_paid_minutes_used <: int?:
            @json_tag = "total_paid_minutes_used"

    !type actions-billing-usage_minutes_used_breakdown:
        MACOS <: int?:
            @json_tag = "MACOS"
        UBUNTU <: int?:
            @json_tag = "UBUNTU"
        WINDOWS <: int?:
            @json_tag = "WINDOWS"

    !type actions-enterprise-permissions:
        allowed_actions <: allowed-actions?:
            @json_tag = "allowed_actions"
        enabled_organizations <: enabled-organizations?:
            @json_tag = "enabled_organizations"
        selected_actions_url <: selected-actions-url?:
            @json_tag = "selected_actions_url"
        selected_organizations_url <: string?:
            @json_tag = "selected_organizations_url"

    !type actions-organization-permissions:
        allowed_actions <: allowed-actions?:
            @json_tag = "allowed_actions"
        enabled_repositories <: enabled-repositories?:
            @json_tag = "enabled_repositories"
        selected_actions_url <: selected-actions-url?:
            @json_tag = "selected_actions_url"
        selected_repositories_url <: string?:
            @json_tag = "selected_repositories_url"

    !type actions-public-key:
        created_at <: string?:
            @json_tag = "created_at"
        id <: int?:
            @json_tag = "id"
        key <: string:
            @json_tag = "key"
        key_id <: string:
            @json_tag = "key_id"
        title <: string?:
            @json_tag = "title"
        url <: string?:
            @json_tag = "url"

    !type actions-repository-permissions:
        allowed_actions <: allowed-actions?:
            @json_tag = "allowed_actions"
        enabled <: actions-enabled?:
            @json_tag = "enabled"
        selected_actions_url <: selected-actions-url?:
            @json_tag = "selected_actions_url"

    !type actions-secret:
        created_at <: datetime:
            @json_tag = "created_at"
        name <: string:
            @json_tag = "name"
        updated_at <: datetime:
            @json_tag = "updated_at"

    !type actor:
        avatar_url <: string:
            @json_tag = "avatar_url"
        display_login <: string?:
            @json_tag = "display_login"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        url <: string:
            @json_tag = "url"

    !alias allowed-actions:
        str

    !type api-overview:
        api <: sequence of string?:
            @json_tag = "api"
        git <: sequence of string?:
            @json_tag = "git"
        github_services_sha <: string?:
            @json_tag = "github_services_sha"
        hooks <: sequence of string?:
            @json_tag = "hooks"
        importer <: sequence of string?:
            @json_tag = "importer"
        installed_version <: string?:
            @json_tag = "installed_version"
        pages <: sequence of string?:
            @json_tag = "pages"
        ssh_key_fingerprints <: api-overview_ssh_key_fingerprints?:
            @json_tag = "ssh_key_fingerprints"
        verifiable_password_authentication <: bool:
            @json_tag = "verifiable_password_authentication"
        web <: sequence of string?:
            @json_tag = "web"

    !type api-overview_ssh_key_fingerprints:
        MD5_DSA <: string?:
            @json_tag = "MD5_DSA"
        MD5_RSA <: string?:
            @json_tag = "MD5_RSA"
        SHA256_DSA <: string?:
            @json_tag = "SHA256_DSA"
        SHA256_RSA <: string?:
            @json_tag = "SHA256_RSA"

    !type application-grant:
        app <: application-grant_app:
            @json_tag = "app"
        created_at <: datetime:
            @json_tag = "created_at"
        id <: int:
            @json_tag = "id"
        scopes <: sequence of string:
            @json_tag = "scopes"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_application-grant_user?:
            @json_tag = "user"

    !type application-grant_app:
        client_id <: string:
            @json_tag = "client_id"
        name <: string:
            @json_tag = "name"
        url <: string:
            @json_tag = "url"

    !type artifact:
        archive_download_url <: string:
            @json_tag = "archive_download_url"
        created_at <: datetime:
            @json_tag = "created_at"
        expired <: bool:
            @json_tag = "expired"
        expires_at <: datetime:
            @json_tag = "expires_at"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        size_in_bytes <: int:
            @json_tag = "size_in_bytes"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type authentication-token:
        expires_at <: datetime:
            @json_tag = "expires_at"
        permissions <: EXTERNAL_authentication-token_permissions?:
            @json_tag = "permissions"
        repositories <: sequence of repository?:
            @json_tag = "repositories"
        repository_selection <: string?:
            @json_tag = "repository_selection"
        single_file <: string?:
            @json_tag = "single_file"
        token <: string:
            @json_tag = "token"

    !type authorization:
        app <: authorization_app:
            @json_tag = "app"
        created_at <: datetime:
            @json_tag = "created_at"
        fingerprint <: string:
            @json_tag = "fingerprint"
        hashed_token <: string:
            @json_tag = "hashed_token"
        id <: int:
            @json_tag = "id"
        installation <: EXTERNAL_authorization_installation?:
            @json_tag = "installation"
        note <: string:
            @json_tag = "note"
        note_url <: string:
            @json_tag = "note_url"
        scopes <: sequence of string:
            @json_tag = "scopes"
        token <: string:
            @json_tag = "token"
        token_last_eight <: string:
            @json_tag = "token_last_eight"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_authorization_user?:
            @json_tag = "user"

    !type authorization_app:
        client_id <: string:
            @json_tag = "client_id"
        name <: string:
            @json_tag = "name"
        url <: string:
            @json_tag = "url"

    !type base-gist:
        comments <: int:
            @json_tag = "comments"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        created_at <: datetime:
            @json_tag = "created_at"
        description <: string:
            @json_tag = "description"
        files <: EXTERNAL_base-gist_files:
            @json_tag = "files"
        forks <: sequence of EXTERNAL_base-gist_forks?:
            @json_tag = "forks"
        forks_url <: string:
            @json_tag = "forks_url"
        git_pull_url <: string:
            @json_tag = "git_pull_url"
        git_push_url <: string:
            @json_tag = "git_push_url"
        history <: sequence of EXTERNAL_base-gist_history?:
            @json_tag = "history"
        html_url <: string:
            @json_tag = "html_url"
        id <: string:
            @json_tag = "id"
        node_id <: string:
            @json_tag = "node_id"
        owner <: EXTERNAL_base-gist_owner?:
            @json_tag = "owner"
        public <: bool:
            @json_tag = "public"
        truncated <: bool?:
            @json_tag = "truncated"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_base-gist_user:
            @json_tag = "user"

    !type basic-error:
        documentation_url <: string?:
            @json_tag = "documentation_url"
        message <: string?:
            @json_tag = "message"

    !type blob:
        content <: string:
            @json_tag = "content"
        encoding <: string:
            @json_tag = "encoding"
        highlighted_content <: string?:
            @json_tag = "highlighted_content"
        node_id <: string:
            @json_tag = "node_id"
        sha <: string:
            @json_tag = "sha"
        size <: int:
            @json_tag = "size"
        url <: string:
            @json_tag = "url"

    !type branch-protection:
        allow_deletions <: branch-protection_allow_deletions?:
            @json_tag = "allow_deletions"
        allow_force_pushes <: branch-protection_allow_force_pushes?:
            @json_tag = "allow_force_pushes"
        enabled <: bool:
            @json_tag = "enabled"
        enforce_admins <: protected-branch-admin-enforced?:
            @json_tag = "enforce_admins"
        name <: string?:
            @json_tag = "name"
        protection_url <: string?:
            @json_tag = "protection_url"
        required_linear_history <: branch-protection_required_linear_history?:
            @json_tag = "required_linear_history"
        required_pull_request_reviews <: protected-branch-pull-request-review?:
            @json_tag = "required_pull_request_reviews"
        required_status_checks <: branch-protection_required_status_checks:
            @json_tag = "required_status_checks"
        restrictions <: branch-restriction-policy?:
            @json_tag = "restrictions"
        url <: string?:
            @json_tag = "url"

    !type branch-protection_allow_deletions:
        enabled <: bool?:
            @json_tag = "enabled"

    !type branch-protection_allow_force_pushes:
        enabled <: bool?:
            @json_tag = "enabled"

    !type branch-protection_required_linear_history:
        enabled <: bool?:
            @json_tag = "enabled"

    !type branch-protection_required_status_checks:
        contexts <: sequence of string:
            @json_tag = "contexts"
        contexts_url <: string?:
            @json_tag = "contexts_url"
        enforcement_level <: string:
            @json_tag = "enforcement_level"
        url <: string?:
            @json_tag = "url"

    !type branch-restriction-policy:
        apps <: sequence of branch-restriction-policy_apps:
            @json_tag = "apps"
        apps_url <: string:
            @json_tag = "apps_url"
        teams <: sequence of branch-restriction-policy_teams:
            @json_tag = "teams"
        teams_url <: string:
            @json_tag = "teams_url"
        url <: string:
            @json_tag = "url"
        users <: sequence of branch-restriction-policy_users:
            @json_tag = "users"
        users_url <: string:
            @json_tag = "users_url"

    !type branch-restriction-policy_apps:
        created_at <: string?:
            @json_tag = "created_at"
        description <: string?:
            @json_tag = "description"
        events <: sequence of string?:
            @json_tag = "events"
        external_url <: string?:
            @json_tag = "external_url"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        owner <: branch-restriction-policy_apps_owner?:
            @json_tag = "owner"
        permissions <: branch-restriction-policy_apps_permissions?:
            @json_tag = "permissions"
        slug <: string?:
            @json_tag = "slug"
        updated_at <: string?:
            @json_tag = "updated_at"

    !type branch-restriction-policy_apps_owner:
        avatar_url <: string?:
            @json_tag = "avatar_url"
        description <: string?:
            @json_tag = "description"
        events_url <: string?:
            @json_tag = "events_url"
        followers_url <: string?:
            @json_tag = "followers_url"
        following_url <: string?:
            @json_tag = "following_url"
        gists_url <: string?:
            @json_tag = "gists_url"
        gravatar_id <: string?:
            @json_tag = "gravatar_id"
        hooks_url <: string?:
            @json_tag = "hooks_url"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        issues_url <: string?:
            @json_tag = "issues_url"
        login <: string?:
            @json_tag = "login"
        members_url <: string?:
            @json_tag = "members_url"
        node_id <: string?:
            @json_tag = "node_id"
        organizations_url <: string?:
            @json_tag = "organizations_url"
        public_members_url <: string?:
            @json_tag = "public_members_url"
        received_events_url <: string?:
            @json_tag = "received_events_url"
        repos_url <: string?:
            @json_tag = "repos_url"
        starred_url <: string?:
            @json_tag = "starred_url"
        subscriptions_url <: string?:
            @json_tag = "subscriptions_url"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type branch-restriction-policy_apps_permissions:
        contents <: string?:
            @json_tag = "contents"
        issues <: string?:
            @json_tag = "issues"
        metadata <: string?:
            @json_tag = "metadata"
        single_file <: string?:
            @json_tag = "single_file"

    !type branch-restriction-policy_teams:
        description <: string?:
            @json_tag = "description"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        members_url <: string?:
            @json_tag = "members_url"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        parent <: string?:
            @json_tag = "parent"
        permission <: string?:
            @json_tag = "permission"
        privacy <: string?:
            @json_tag = "privacy"
        repositories_url <: string?:
            @json_tag = "repositories_url"
        slug <: string?:
            @json_tag = "slug"
        url <: string?:
            @json_tag = "url"

    !type branch-restriction-policy_users:
        avatar_url <: string?:
            @json_tag = "avatar_url"
        events_url <: string?:
            @json_tag = "events_url"
        followers_url <: string?:
            @json_tag = "followers_url"
        following_url <: string?:
            @json_tag = "following_url"
        gists_url <: string?:
            @json_tag = "gists_url"
        gravatar_id <: string?:
            @json_tag = "gravatar_id"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        login <: string?:
            @json_tag = "login"
        node_id <: string?:
            @json_tag = "node_id"
        organizations_url <: string?:
            @json_tag = "organizations_url"
        received_events_url <: string?:
            @json_tag = "received_events_url"
        repos_url <: string?:
            @json_tag = "repos_url"
        site_admin <: bool?:
            @json_tag = "site_admin"
        starred_url <: string?:
            @json_tag = "starred_url"
        subscriptions_url <: string?:
            @json_tag = "subscriptions_url"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type branch-short:
        commit <: branch-short_commit?:
            @json_tag = "commit"
        name <: string?:
            @json_tag = "name"
        protected <: bool?:
            @json_tag = "protected"

    !type branch-short_commit:
        sha <: string?:
            @json_tag = "sha"
        url <: string?:
            @json_tag = "url"

    !type branch-with-protection:
        _links <: branch-with-protection__links:
            @json_tag = "_links"
        commit <: commit:
            @json_tag = "commit"
        name <: string:
            @json_tag = "name"
        pattern <: string?:
            @json_tag = "pattern"
        protected <: bool:
            @json_tag = "protected"
        protection <: branch-protection:
            @json_tag = "protection"
        protection_url <: string:
            @json_tag = "protection_url"
        required_approving_review_count <: int?:
            @json_tag = "required_approving_review_count"

    !type branch-with-protection__links:
        html <: string:
            @json_tag = "html"
        self <: string:
            @json_tag = "self"

    !type check-annotation:
        annotation_level <: string:
            @json_tag = "annotation_level"
        blob_href <: string:
            @json_tag = "blob_href"
        end_column <: int:
            @json_tag = "end_column"
        end_line <: int:
            @json_tag = "end_line"
        message <: string:
            @json_tag = "message"
        path <: string:
            @json_tag = "path"
        raw_details <: string:
            @json_tag = "raw_details"
        start_column <: int:
            @json_tag = "start_column"
        start_line <: int:
            @json_tag = "start_line"
        title <: string:
            @json_tag = "title"

    !type check-run:
        app <: EXTERNAL_check-run_app:
            @json_tag = "app"
        check_suite <: check-run_check_suite:
            @json_tag = "check_suite"
        completed_at <: datetime:
            @json_tag = "completed_at"
        conclusion <: string:
            @json_tag = "conclusion"
        details_url <: string:
            @json_tag = "details_url"
        external_id <: string:
            @json_tag = "external_id"
        head_sha <: string:
            @json_tag = "head_sha"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        output <: check-run_output:
            @json_tag = "output"
        pull_requests <: EXTERNAL_check-run_pull_requests:
            @json_tag = "pull_requests"
        started_at <: datetime:
            @json_tag = "started_at"
        status <: string:
            @json_tag = "status"
        url <: string:
            @json_tag = "url"

    !type check-run_check_suite:
        id <: int:
            @json_tag = "id"

    !type check-run_output:
        annotations_count <: int:
            @json_tag = "annotations_count"
        annotations_url <: string:
            @json_tag = "annotations_url"
        summary <: string:
            @json_tag = "summary"
        text <: string:
            @json_tag = "text"
        title <: string:
            @json_tag = "title"

    !type check-suite:
        after <: string:
            @json_tag = "after"
        app <: EXTERNAL_check-suite_app:
            @json_tag = "app"
        before <: string:
            @json_tag = "before"
        check_runs_url <: string:
            @json_tag = "check_runs_url"
        conclusion <: string:
            @json_tag = "conclusion"
        created_at <: datetime:
            @json_tag = "created_at"
        head_branch <: string:
            @json_tag = "head_branch"
        head_commit <: simple-commit:
            @json_tag = "head_commit"
        head_sha <: string:
            @json_tag = "head_sha"
        id <: int:
            @json_tag = "id"
        latest_check_runs_count <: int:
            @json_tag = "latest_check_runs_count"
        node_id <: string:
            @json_tag = "node_id"
        pull_requests <: sequence of pull-request-minimal:
            @json_tag = "pull_requests"
        repository <: minimal-repository:
            @json_tag = "repository"
        status <: string:
            @json_tag = "status"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type check-suite-preference:
        preferences <: check-suite-preference_preferences?:
            @json_tag = "preferences"
        repository <: repository?:
            @json_tag = "repository"

    !type check-suite-preference_preferences:
        auto_trigger_checks <: sequence of check-suite-preference_preferences_auto_trigger_checks?:
            @json_tag = "auto_trigger_checks"

    !type check-suite-preference_preferences_auto_trigger_checks:
        app_id <: int:
            @json_tag = "app_id"
        setting <: bool:
            @json_tag = "setting"

    !type clone-traffic:
        clones <: sequence of traffic:
            @json_tag = "clones"
        count <: int:
            @json_tag = "count"
        uniques <: int:
            @json_tag = "uniques"

    !type code-of-conduct:
        body <: string?:
            @json_tag = "body"
        html_url <: string:
            @json_tag = "html_url"
        key <: string:
            @json_tag = "key"
        name <: string:
            @json_tag = "name"
        url <: string:
            @json_tag = "url"

    !type code-of-conduct-simple:
        html_url <: string:
            @json_tag = "html_url"
        key <: string:
            @json_tag = "key"
        name <: string:
            @json_tag = "name"
        url <: string:
            @json_tag = "url"

    !type code-scanning-alert-code-scanning-alert:
        created_at <: code-scanning-alert-created-at?:
            @json_tag = "created_at"
        dismissed_at <: code-scanning-alert-dismissed-at?:
            @json_tag = "dismissed_at"
        dismissed_by <: code-scanning-alert-dismissed-by?:
            @json_tag = "dismissed_by"
        dismissed_reason <: code-scanning-alert-dismissed-reason?:
            @json_tag = "dismissed_reason"
        html_url <: code-scanning-alert-html-url?:
            @json_tag = "html_url"
        instances <: code-scanning-alert-instances?:
            @json_tag = "instances"
        number <: code-scanning-alert-number?:
            @json_tag = "number"
        rule <: code-scanning-alert-rule?:
            @json_tag = "rule"
        state <: code-scanning-alert-state?:
            @json_tag = "state"
        tool <: code-scanning-analysis-tool?:
            @json_tag = "tool"
        url <: code-scanning-alert-url?:
            @json_tag = "url"

    !type code-scanning-alert-code-scanning-alert-items:
        created_at <: code-scanning-alert-created-at?:
            @json_tag = "created_at"
        dismissed_at <: code-scanning-alert-dismissed-at?:
            @json_tag = "dismissed_at"
        dismissed_by <: code-scanning-alert-dismissed-by?:
            @json_tag = "dismissed_by"
        dismissed_reason <: code-scanning-alert-dismissed-reason?:
            @json_tag = "dismissed_reason"
        html_url <: code-scanning-alert-html-url?:
            @json_tag = "html_url"
        number <: code-scanning-alert-number?:
            @json_tag = "number"
        rule <: code-scanning-alert-rule?:
            @json_tag = "rule"
        state <: code-scanning-alert-state?:
            @json_tag = "state"
        tool <: code-scanning-analysis-tool?:
            @json_tag = "tool"
        url <: code-scanning-alert-url?:
            @json_tag = "url"

    !union code-scanning-alert-dismissed-by:
        simple-user
        object

    !type code-scanning-alert-instances_obj:
        analysis_key <: code-scanning-analysis-analysis-key?:
            @json_tag = "analysis_key"
        environment <: code-scanning-alert-environment?:
            @json_tag = "environment"
        matrix_vars <: string?:
            @json_tag = "matrix_vars"
        ref <: code-scanning-alert-ref?:
            @json_tag = "ref"
        state <: code-scanning-alert-state?:
            @json_tag = "state"

    !type code-scanning-alert-rule:
        description <: string?:
            @json_tag = "description"
        id <: string?:
            @json_tag = "id"
        severity <: string?:
            @json_tag = "severity"

    !alias code-scanning-alert-set-state:
        str

    !alias code-scanning-alert-state:
        str

    !type code-scanning-analysis-code-scanning-analysis:
        analysis_key <: code-scanning-analysis-analysis-key?:
            @json_tag = "analysis_key"
        commit_sha <: code-scanning-analysis-commit-sha?:
            @json_tag = "commit_sha"
        created_at <: code-scanning-analysis-created-at?:
            @json_tag = "created_at"
        environment <: code-scanning-analysis-environment?:
            @json_tag = "environment"
        error <: string?:
            @json_tag = "error"
        ref <: code-scanning-analysis-ref?:
            @json_tag = "ref"
        tool_name <: code-scanning-analysis-tool-name?:
            @json_tag = "tool_name"

    !type code-scanning-analysis-tool:
        name <: code-scanning-analysis-tool-name?:
            @json_tag = "name"
        version <: string?:
            @json_tag = "version"

    !type code-search-result-item:
        file_size <: int?:
            @json_tag = "file_size"
        git_url <: string:
            @json_tag = "git_url"
        html_url <: string:
            @json_tag = "html_url"
        language <: string?:
            @json_tag = "language"
        last_modified_at <: datetime?:
            @json_tag = "last_modified_at"
        line_numbers <: sequence of string?:
            @json_tag = "line_numbers"
        name <: string:
            @json_tag = "name"
        path <: string:
            @json_tag = "path"
        repository <: minimal-repository:
            @json_tag = "repository"
        score <: int:
            @json_tag = "score"
        sha <: string:
            @json_tag = "sha"
        text_matches <: search-result-text-matches?:
            @json_tag = "text_matches"
        url <: string:
            @json_tag = "url"

    !type collaborator:
        avatar_url <: string:
            @json_tag = "avatar_url"
        events_url <: string:
            @json_tag = "events_url"
        followers_url <: string:
            @json_tag = "followers_url"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        permissions <: collaborator_permissions?:
            @json_tag = "permissions"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type collaborator_permissions:
        admin <: bool:
            @json_tag = "admin"
        pull <: bool:
            @json_tag = "pull"
        push <: bool:
            @json_tag = "push"

    !type combined-billing-usage:
        days_left_in_billing_cycle <: int?:
            @json_tag = "days_left_in_billing_cycle"
        estimated_paid_storage_for_month <: int?:
            @json_tag = "estimated_paid_storage_for_month"
        estimated_storage_for_month <: int?:
            @json_tag = "estimated_storage_for_month"

    !type combined-commit-status:
        commit_url <: string:
            @json_tag = "commit_url"
        repository <: minimal-repository:
            @json_tag = "repository"
        sha <: string:
            @json_tag = "sha"
        state <: string:
            @json_tag = "state"
        statuses <: sequence of simple-commit-status:
            @json_tag = "statuses"
        total_count <: int:
            @json_tag = "total_count"
        url <: string:
            @json_tag = "url"

    !type commit:
        author <: EXTERNAL_commit_author:
            @json_tag = "author"
        comments_url <: string:
            @json_tag = "comments_url"
        commit <: commit_commit:
            @json_tag = "commit"
        committer <: EXTERNAL_commit_committer:
            @json_tag = "committer"
        files <: sequence of commit_files?:
            @json_tag = "files"
        html_url <: string:
            @json_tag = "html_url"
        node_id <: string:
            @json_tag = "node_id"
        parents <: sequence of commit_parents:
            @json_tag = "parents"
        sha <: string:
            @json_tag = "sha"
        stats <: commit_stats?:
            @json_tag = "stats"
        url <: string:
            @json_tag = "url"

    !type commit-activity:
        days <: sequence of int:
            @json_tag = "days"
        total <: int:
            @json_tag = "total"
        week <: int:
            @json_tag = "week"

    !type commit-comment:
        author_association <: string:
            @json_tag = "author_association"
        body <: string:
            @json_tag = "body"
        commit_id <: string:
            @json_tag = "commit_id"
        created_at <: datetime:
            @json_tag = "created_at"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        line <: int:
            @json_tag = "line"
        node_id <: string:
            @json_tag = "node_id"
        path <: string:
            @json_tag = "path"
        position <: int:
            @json_tag = "position"
        reactions <: reaction-rollup?:
            @json_tag = "reactions"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_commit-comment_user:
            @json_tag = "user"

    !type commit-comparison:
        ahead_by <: int:
            @json_tag = "ahead_by"
        base_commit <: commit:
            @json_tag = "base_commit"
        behind_by <: int:
            @json_tag = "behind_by"
        commits <: sequence of commit:
            @json_tag = "commits"
        diff_url <: string:
            @json_tag = "diff_url"
        files <: sequence of diff-entry:
            @json_tag = "files"
        html_url <: string:
            @json_tag = "html_url"
        merge_base_commit <: commit:
            @json_tag = "merge_base_commit"
        patch_url <: string:
            @json_tag = "patch_url"
        permalink_url <: string:
            @json_tag = "permalink_url"
        status <: string:
            @json_tag = "status"
        total_commits <: int:
            @json_tag = "total_commits"
        url <: string:
            @json_tag = "url"

    !type commit-search-result-item:
        author <: EXTERNAL_commit-search-result-item_author:
            @json_tag = "author"
        comments_url <: string:
            @json_tag = "comments_url"
        commit <: commit-search-result-item_commit:
            @json_tag = "commit"
        committer <: EXTERNAL_commit-search-result-item_committer:
            @json_tag = "committer"
        html_url <: string:
            @json_tag = "html_url"
        node_id <: string:
            @json_tag = "node_id"
        parents <: sequence of commit-search-result-item_parents:
            @json_tag = "parents"
        repository <: minimal-repository:
            @json_tag = "repository"
        score <: int:
            @json_tag = "score"
        sha <: string:
            @json_tag = "sha"
        text_matches <: search-result-text-matches?:
            @json_tag = "text_matches"
        url <: string:
            @json_tag = "url"

    !type commit-search-result-item_commit:
        author <: commit-search-result-item_commit_author:
            @json_tag = "author"
        comment_count <: int:
            @json_tag = "comment_count"
        committer <: EXTERNAL_commit-search-result-item_commit_committer:
            @json_tag = "committer"
        message <: string:
            @json_tag = "message"
        tree <: commit-search-result-item_commit_tree:
            @json_tag = "tree"
        url <: string:
            @json_tag = "url"
        verification <: verification?:
            @json_tag = "verification"

    !type commit-search-result-item_commit_author:
        date_ <: datetime:
            @json_tag = "date"
        email <: string:
            @json_tag = "email"
        name <: string:
            @json_tag = "name"

    !type commit-search-result-item_commit_tree:
        sha <: string:
            @json_tag = "sha"
        url <: string:
            @json_tag = "url"

    !type commit-search-result-item_parents:
        html_url <: string?:
            @json_tag = "html_url"
        sha <: string?:
            @json_tag = "sha"
        url <: string?:
            @json_tag = "url"

    !type commit_commit:
        author <: EXTERNAL_commit_commit_author:
            @json_tag = "author"
        comment_count <: int:
            @json_tag = "comment_count"
        committer <: EXTERNAL_commit_commit_committer:
            @json_tag = "committer"
        message <: string:
            @json_tag = "message"
        tree <: commit_commit_tree:
            @json_tag = "tree"
        url <: string:
            @json_tag = "url"
        verification <: verification?:
            @json_tag = "verification"

    !type commit_commit_tree:
        sha <: string:
            @json_tag = "sha"
        url <: string:
            @json_tag = "url"

    !type commit_files:
        additions <: int?:
            @json_tag = "additions"
        blob_url <: string?:
            @json_tag = "blob_url"
        changes <: int?:
            @json_tag = "changes"
        contents_url <: string?:
            @json_tag = "contents_url"
        deletions <: int?:
            @json_tag = "deletions"
        filename <: string?:
            @json_tag = "filename"
        patch <: string?:
            @json_tag = "patch"
        previous_filename <: string?:
            @json_tag = "previous_filename"
        raw_url <: string?:
            @json_tag = "raw_url"
        sha <: string?:
            @json_tag = "sha"
        status <: string?:
            @json_tag = "status"

    !type commit_parents:
        html_url <: string?:
            @json_tag = "html_url"
        sha <: string:
            @json_tag = "sha"
        url <: string:
            @json_tag = "url"

    !type commit_stats:
        additions <: int?:
            @json_tag = "additions"
        deletions <: int?:
            @json_tag = "deletions"
        total <: int?:
            @json_tag = "total"

    !type community-health-file:
        html_url <: string:
            @json_tag = "html_url"
        url <: string:
            @json_tag = "url"

    !type community-profile:
        description <: string:
            @json_tag = "description"
        documentation <: string:
            @json_tag = "documentation"
        files <: community-profile_files:
            @json_tag = "files"
        health_percentage <: int:
            @json_tag = "health_percentage"
        updated_at <: datetime:
            @json_tag = "updated_at"

    !type community-profile_files:
        code_of_conduct <: EXTERNAL_community-profile_files_code_of_conduct:
            @json_tag = "code_of_conduct"
        contributing <: EXTERNAL_community-profile_files_contributing:
            @json_tag = "contributing"
        issue_template <: EXTERNAL_community-profile_files_issue_template:
            @json_tag = "issue_template"
        license <: EXTERNAL_community-profile_files_license:
            @json_tag = "license"
        pull_request_template <: EXTERNAL_community-profile_files_pull_request_template:
            @json_tag = "pull_request_template"
        readme <: EXTERNAL_community-profile_files_readme:
            @json_tag = "readme"

    !type content-directory_obj:
        _links <: content-directory_obj_content-directory_obj__links:
            @json_tag = "_links"
        content <: string?:
            @json_tag = "content"
        download_url <: string:
            @json_tag = "download_url"
        git_url <: string:
            @json_tag = "git_url"
        html_url <: string:
            @json_tag = "html_url"
        name <: string:
            @json_tag = "name"
        path <: string:
            @json_tag = "path"
        sha <: string:
            @json_tag = "sha"
        size <: int:
            @json_tag = "size"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type content-directory_obj_content-directory_obj__links:
        git <: string:
            @json_tag = "git"
        html <: string:
            @json_tag = "html"
        self <: string:
            @json_tag = "self"

    !type content-file:
        _links <: content-file__links:
            @json_tag = "_links"
        content <: string:
            @json_tag = "content"
        download_url <: string:
            @json_tag = "download_url"
        encoding <: string:
            @json_tag = "encoding"
        git_url <: string:
            @json_tag = "git_url"
        html_url <: string:
            @json_tag = "html_url"
        name <: string:
            @json_tag = "name"
        path <: string:
            @json_tag = "path"
        sha <: string:
            @json_tag = "sha"
        size <: int:
            @json_tag = "size"
        submodule_git_url <: string?:
            @json_tag = "submodule_git_url"
        target <: string?:
            @json_tag = "target"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type content-file__links:
        git <: string:
            @json_tag = "git"
        html <: string:
            @json_tag = "html"
        self <: string:
            @json_tag = "self"

    !type content-reference-attachment:
        body <: string:
            @json_tag = "body"
        id <: int:
            @json_tag = "id"
        node_id <: string?:
            @json_tag = "node_id"
        title <: string:
            @json_tag = "title"

    !type content-submodule:
        _links <: content-submodule__links:
            @json_tag = "_links"
        download_url <: string:
            @json_tag = "download_url"
        git_url <: string:
            @json_tag = "git_url"
        html_url <: string:
            @json_tag = "html_url"
        name <: string:
            @json_tag = "name"
        path <: string:
            @json_tag = "path"
        sha <: string:
            @json_tag = "sha"
        size <: int:
            @json_tag = "size"
        submodule_git_url <: string:
            @json_tag = "submodule_git_url"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type content-submodule__links:
        git <: string:
            @json_tag = "git"
        html <: string:
            @json_tag = "html"
        self <: string:
            @json_tag = "self"

    !type content-symlink:
        _links <: content-symlink__links:
            @json_tag = "_links"
        download_url <: string:
            @json_tag = "download_url"
        git_url <: string:
            @json_tag = "git_url"
        html_url <: string:
            @json_tag = "html_url"
        name <: string:
            @json_tag = "name"
        path <: string:
            @json_tag = "path"
        sha <: string:
            @json_tag = "sha"
        size <: int:
            @json_tag = "size"
        target <: string:
            @json_tag = "target"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type content-symlink__links:
        git <: string:
            @json_tag = "git"
        html <: string:
            @json_tag = "html"
        self <: string:
            @json_tag = "self"

    !type content-traffic:
        count <: int:
            @json_tag = "count"
        path <: string:
            @json_tag = "path"
        title <: string:
            @json_tag = "title"
        uniques <: int:
            @json_tag = "uniques"

    !type content-tree:
        _links <: content-tree__links:
            @json_tag = "_links"
        download_url <: string:
            @json_tag = "download_url"
        entries <: sequence of content-tree_entries?:
            @json_tag = "entries"
        git_url <: string:
            @json_tag = "git_url"
        html_url <: string:
            @json_tag = "html_url"
        name <: string:
            @json_tag = "name"
        path <: string:
            @json_tag = "path"
        sha <: string:
            @json_tag = "sha"
        size <: int:
            @json_tag = "size"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type content-tree__links:
        git <: string:
            @json_tag = "git"
        html <: string:
            @json_tag = "html"
        self <: string:
            @json_tag = "self"

    !type content-tree_entries:
        _links <: content-tree_entries__links:
            @json_tag = "_links"
        content <: string?:
            @json_tag = "content"
        download_url <: string:
            @json_tag = "download_url"
        git_url <: string:
            @json_tag = "git_url"
        html_url <: string:
            @json_tag = "html_url"
        name <: string:
            @json_tag = "name"
        path <: string:
            @json_tag = "path"
        sha <: string:
            @json_tag = "sha"
        size <: int:
            @json_tag = "size"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type content-tree_entries__links:
        git <: string:
            @json_tag = "git"
        html <: string:
            @json_tag = "html"
        self <: string:
            @json_tag = "self"

    !type contributor:
        avatar_url <: string?:
            @json_tag = "avatar_url"
        contributions <: int:
            @json_tag = "contributions"
        email <: string?:
            @json_tag = "email"
        events_url <: string?:
            @json_tag = "events_url"
        followers_url <: string?:
            @json_tag = "followers_url"
        following_url <: string?:
            @json_tag = "following_url"
        gists_url <: string?:
            @json_tag = "gists_url"
        gravatar_id <: string?:
            @json_tag = "gravatar_id"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        login <: string?:
            @json_tag = "login"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        organizations_url <: string?:
            @json_tag = "organizations_url"
        received_events_url <: string?:
            @json_tag = "received_events_url"
        repos_url <: string?:
            @json_tag = "repos_url"
        site_admin <: bool?:
            @json_tag = "site_admin"
        starred_url <: string?:
            @json_tag = "starred_url"
        subscriptions_url <: string?:
            @json_tag = "subscriptions_url"
        type <: string:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type contributor-activity:
        author <: EXTERNAL_contributor-activity_author:
            @json_tag = "author"
        total <: int:
            @json_tag = "total"
        weeks <: sequence of contributor-activity_weeks:
            @json_tag = "weeks"

    !type contributor-activity_weeks:
        a <: int?:
            @json_tag = "a"
        c <: int?:
            @json_tag = "c"
        d <: int?:
            @json_tag = "d"
        w <: string?:
            @json_tag = "w"

    !type credential-authorization:
        credential_accessed_at <: datetime?:
            @json_tag = "credential_accessed_at"
        credential_authorized_at <: datetime:
            @json_tag = "credential_authorized_at"
        credential_id <: int:
            @json_tag = "credential_id"
        credential_type <: string:
            @json_tag = "credential_type"
        fingerprint <: string?:
            @json_tag = "fingerprint"
        login <: string:
            @json_tag = "login"
        scopes <: sequence of string?:
            @json_tag = "scopes"
        token_last_eight <: string?:
            @json_tag = "token_last_eight"

    !type deploy-key:
        created_at <: string?:
            @json_tag = "created_at"
        id <: int?:
            @json_tag = "id"
        key <: string?:
            @json_tag = "key"
        read_only <: bool?:
            @json_tag = "read_only"
        title <: string?:
            @json_tag = "title"
        url <: string?:
            @json_tag = "url"
        verified <: bool?:
            @json_tag = "verified"

    !type deployment:
        created_at <: datetime:
            @json_tag = "created_at"
        creator <: EXTERNAL_deployment_creator:
            @json_tag = "creator"
        description <: string:
            @json_tag = "description"
        environment <: string:
            @json_tag = "environment"
        id <: int:
            @json_tag = "id"
        node_id <: string:
            @json_tag = "node_id"
        original_environment <: string?:
            @json_tag = "original_environment"
        payload <: EXTERNAL_deployment_payload:
            @json_tag = "payload"
        performed_via_github_app <: EXTERNAL_deployment_performed_via_github_app?:
            @json_tag = "performed_via_github_app"
        production_environment <: bool?:
            @json_tag = "production_environment"
        ref <: string:
            @json_tag = "ref"
        repository_url <: string:
            @json_tag = "repository_url"
        sha <: string:
            @json_tag = "sha"
        statuses_url <: string:
            @json_tag = "statuses_url"
        task <: string:
            @json_tag = "task"
        transient_environment <: bool?:
            @json_tag = "transient_environment"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type deployment-status:
        created_at <: datetime:
            @json_tag = "created_at"
        creator <: EXTERNAL_deployment-status_creator:
            @json_tag = "creator"
        deployment_url <: string:
            @json_tag = "deployment_url"
        description <: string:
            @json_tag = "description"
        environment <: string?:
            @json_tag = "environment"
        environment_url <: string?:
            @json_tag = "environment_url"
        id <: int:
            @json_tag = "id"
        log_url <: string?:
            @json_tag = "log_url"
        node_id <: string:
            @json_tag = "node_id"
        performed_via_github_app <: EXTERNAL_deployment-status_performed_via_github_app?:
            @json_tag = "performed_via_github_app"
        repository_url <: string:
            @json_tag = "repository_url"
        state <: string:
            @json_tag = "state"
        target_url <: string:
            @json_tag = "target_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type diff-entry:
        additions <: int:
            @json_tag = "additions"
        blob_url <: string:
            @json_tag = "blob_url"
        changes <: int:
            @json_tag = "changes"
        contents_url <: string:
            @json_tag = "contents_url"
        deletions <: int:
            @json_tag = "deletions"
        filename <: string:
            @json_tag = "filename"
        patch <: string?:
            @json_tag = "patch"
        previous_filename <: string?:
            @json_tag = "previous_filename"
        raw_url <: string:
            @json_tag = "raw_url"
        sha <: string:
            @json_tag = "sha"
        status <: string:
            @json_tag = "status"

    !alias str:
        string

    !union email:
        object
        str

    !type email_object:
        email <: string:
            @json_tag = "email"
        primary <: bool:
            @json_tag = "primary"
        verified <: bool:
            @json_tag = "verified"
        visibility <: string:
            @json_tag = "visibility"

    !alias enabled-organizations:
        str

    !alias enabled-repositories:
        str

    !type enterprise:
        avatar_url <: string:
            @json_tag = "avatar_url"
        created_at <: datetime:
            @json_tag = "created_at"
        description <: string?:
            @json_tag = "description"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        slug <: string:
            @json_tag = "slug"
        updated_at <: datetime:
            @json_tag = "updated_at"
        website_url <: string?:
            @json_tag = "website_url"

    !type event:
        actor <: actor:
            @json_tag = "actor"
        created_at <: datetime:
            @json_tag = "created_at"
        id <: string:
            @json_tag = "id"
        org <: actor?:
            @json_tag = "org"
        payload <: event_payload:
            @json_tag = "payload"
        public <: bool:
            @json_tag = "public"
        repo <: event_repo:
            @json_tag = "repo"
        type <: string:
            @json_tag = "type"

    !type event_payload:
        action <: string:
            @json_tag = "action"
        comment <: issue-comment?:
            @json_tag = "comment"
        issue <: issue-simple?:
            @json_tag = "issue"
        pages <: sequence of event_payload_pages?:
            @json_tag = "pages"

    !type event_payload_pages:
        action <: string?:
            @json_tag = "action"
        html_url <: string?:
            @json_tag = "html_url"
        page_name <: string?:
            @json_tag = "page_name"
        sha <: string?:
            @json_tag = "sha"
        summary <: string?:
            @json_tag = "summary"
        title <: string?:
            @json_tag = "title"

    !type event_repo:
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        url <: string:
            @json_tag = "url"

    !type feed:
        _links <: feed__links:
            @json_tag = "_links"
        current_user_actor_url <: string?:
            @json_tag = "current_user_actor_url"
        current_user_organization_url <: string?:
            @json_tag = "current_user_organization_url"
        current_user_organization_urls <: sequence of string?:
            @json_tag = "current_user_organization_urls"
        current_user_public_url <: string?:
            @json_tag = "current_user_public_url"
        current_user_url <: string?:
            @json_tag = "current_user_url"
        security_advisories_url <: string?:
            @json_tag = "security_advisories_url"
        timeline_url <: string:
            @json_tag = "timeline_url"
        user_url <: string:
            @json_tag = "user_url"

    !type feed__links:
        current_user <: link-with-type?:
            @json_tag = "current_user"
        current_user_actor <: link-with-type?:
            @json_tag = "current_user_actor"
        current_user_organization <: link-with-type?:
            @json_tag = "current_user_organization"
        current_user_organizations <: sequence of link-with-type?:
            @json_tag = "current_user_organizations"
        current_user_public <: link-with-type?:
            @json_tag = "current_user_public"
        security_advisories <: link-with-type?:
            @json_tag = "security_advisories"
        timeline <: link-with-type:
            @json_tag = "timeline"
        user <: link-with-type:
            @json_tag = "user"

    !type file-commit:
        commit <: file-commit_commit?:
            @json_tag = "commit"
        content <: file-commit_content?:
            @json_tag = "content"

    !type file-commit_commit:
        author <: file-commit_commit_author?:
            @json_tag = "author"
        committer <: file-commit_commit_committer?:
            @json_tag = "committer"
        html_url <: string?:
            @json_tag = "html_url"
        message <: string?:
            @json_tag = "message"
        node_id <: string?:
            @json_tag = "node_id"
        parents <: sequence of file-commit_commit_parents?:
            @json_tag = "parents"
        sha <: string?:
            @json_tag = "sha"
        tree <: file-commit_commit_tree?:
            @json_tag = "tree"
        url <: string?:
            @json_tag = "url"
        verification <: file-commit_commit_verification?:
            @json_tag = "verification"

    !type file-commit_commit_author:
        date_ <: string?:
            @json_tag = "date"
        email <: string?:
            @json_tag = "email"
        name <: string?:
            @json_tag = "name"

    !type file-commit_commit_committer:
        date_ <: string?:
            @json_tag = "date"
        email <: string?:
            @json_tag = "email"
        name <: string?:
            @json_tag = "name"

    !type file-commit_commit_parents:
        html_url <: string?:
            @json_tag = "html_url"
        sha <: string?:
            @json_tag = "sha"
        url <: string?:
            @json_tag = "url"

    !type file-commit_commit_tree:
        sha <: string?:
            @json_tag = "sha"
        url <: string?:
            @json_tag = "url"

    !type file-commit_commit_verification:
        payload <: string?:
            @json_tag = "payload"
        reason <: string?:
            @json_tag = "reason"
        signature <: string?:
            @json_tag = "signature"
        verified <: bool?:
            @json_tag = "verified"

    !type file-commit_content:
        _links <: file-commit_content__links?:
            @json_tag = "_links"
        download_url <: string?:
            @json_tag = "download_url"
        git_url <: string?:
            @json_tag = "git_url"
        html_url <: string?:
            @json_tag = "html_url"
        name <: string?:
            @json_tag = "name"
        path <: string?:
            @json_tag = "path"
        sha <: string?:
            @json_tag = "sha"
        size <: int?:
            @json_tag = "size"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type file-commit_content__links:
        git <: string?:
            @json_tag = "git"
        html <: string?:
            @json_tag = "html"
        self <: string?:
            @json_tag = "self"

    !type full-repository:
        allow_merge_commit <: bool?:
            @json_tag = "allow_merge_commit"
        allow_rebase_merge <: bool?:
            @json_tag = "allow_rebase_merge"
        allow_squash_merge <: bool?:
            @json_tag = "allow_squash_merge"
        anonymous_access_enabled <: bool?:
            @json_tag = "anonymous_access_enabled"
        archive_url <: string:
            @json_tag = "archive_url"
        archived <: bool:
            @json_tag = "archived"
        assignees_url <: string:
            @json_tag = "assignees_url"
        blobs_url <: string:
            @json_tag = "blobs_url"
        branches_url <: string:
            @json_tag = "branches_url"
        clone_url <: string:
            @json_tag = "clone_url"
        collaborators_url <: string:
            @json_tag = "collaborators_url"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        compare_url <: string:
            @json_tag = "compare_url"
        contents_url <: string:
            @json_tag = "contents_url"
        contributors_url <: string:
            @json_tag = "contributors_url"
        created_at <: datetime:
            @json_tag = "created_at"
        default_branch <: string:
            @json_tag = "default_branch"
        delete_branch_on_merge <: bool?:
            @json_tag = "delete_branch_on_merge"
        deployments_url <: string:
            @json_tag = "deployments_url"
        description <: string:
            @json_tag = "description"
        disabled <: bool:
            @json_tag = "disabled"
        downloads_url <: string:
            @json_tag = "downloads_url"
        events_url <: string:
            @json_tag = "events_url"
        fork <: bool:
            @json_tag = "fork"
        forks <: int:
            @json_tag = "forks"
        forks_count <: int:
            @json_tag = "forks_count"
        forks_url <: string:
            @json_tag = "forks_url"
        full_name <: string:
            @json_tag = "full_name"
        git_commits_url <: string:
            @json_tag = "git_commits_url"
        git_refs_url <: string:
            @json_tag = "git_refs_url"
        git_tags_url <: string:
            @json_tag = "git_tags_url"
        git_url <: string:
            @json_tag = "git_url"
        has_downloads <: bool:
            @json_tag = "has_downloads"
        has_issues <: bool:
            @json_tag = "has_issues"
        has_pages <: bool:
            @json_tag = "has_pages"
        has_projects <: bool:
            @json_tag = "has_projects"
        has_wiki <: bool:
            @json_tag = "has_wiki"
        homepage <: string:
            @json_tag = "homepage"
        hooks_url <: string:
            @json_tag = "hooks_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        is_template <: bool?:
            @json_tag = "is_template"
        issue_comment_url <: string:
            @json_tag = "issue_comment_url"
        issue_events_url <: string:
            @json_tag = "issue_events_url"
        issues_url <: string:
            @json_tag = "issues_url"
        keys_url <: string:
            @json_tag = "keys_url"
        labels_url <: string:
            @json_tag = "labels_url"
        language <: string:
            @json_tag = "language"
        languages_url <: string:
            @json_tag = "languages_url"
        license <: EXTERNAL_full-repository_license:
            @json_tag = "license"
        master_branch <: string?:
            @json_tag = "master_branch"
        merges_url <: string:
            @json_tag = "merges_url"
        milestones_url <: string:
            @json_tag = "milestones_url"
        mirror_url <: string:
            @json_tag = "mirror_url"
        name <: string:
            @json_tag = "name"
        network_count <: int:
            @json_tag = "network_count"
        node_id <: string:
            @json_tag = "node_id"
        notifications_url <: string:
            @json_tag = "notifications_url"
        open_issues <: int:
            @json_tag = "open_issues"
        open_issues_count <: int:
            @json_tag = "open_issues_count"
        organization <: EXTERNAL_full-repository_organization?:
            @json_tag = "organization"
        owner <: EXTERNAL_full-repository_owner:
            @json_tag = "owner"
        parent <: repository?:
            @json_tag = "parent"
        permissions <: full-repository_permissions?:
            @json_tag = "permissions"
        private <: bool:
            @json_tag = "private"
        pulls_url <: string:
            @json_tag = "pulls_url"
        pushed_at <: datetime:
            @json_tag = "pushed_at"
        releases_url <: string:
            @json_tag = "releases_url"
        size <: int:
            @json_tag = "size"
        source <: repository?:
            @json_tag = "source"
        ssh_url <: string:
            @json_tag = "ssh_url"
        stargazers_count <: int:
            @json_tag = "stargazers_count"
        stargazers_url <: string:
            @json_tag = "stargazers_url"
        statuses_url <: string:
            @json_tag = "statuses_url"
        subscribers_count <: int:
            @json_tag = "subscribers_count"
        subscribers_url <: string:
            @json_tag = "subscribers_url"
        subscription_url <: string:
            @json_tag = "subscription_url"
        svn_url <: string:
            @json_tag = "svn_url"
        tags_url <: string:
            @json_tag = "tags_url"
        teams_url <: string:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        template_repository <: EXTERNAL_full-repository_template_repository?:
            @json_tag = "template_repository"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string:
            @json_tag = "trees_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        visibility <: string?:
            @json_tag = "visibility"
        watchers <: int:
            @json_tag = "watchers"
        watchers_count <: int:
            @json_tag = "watchers_count"

    !type full-repository_permissions:
        admin <: bool:
            @json_tag = "admin"
        pull <: bool:
            @json_tag = "pull"
        push <: bool:
            @json_tag = "push"

    !type gist-comment:
        author_association <: string:
            @json_tag = "author_association"
        body <: string:
            @json_tag = "body"
        created_at <: datetime:
            @json_tag = "created_at"
        id <: int:
            @json_tag = "id"
        node_id <: string:
            @json_tag = "node_id"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_gist-comment_user:
            @json_tag = "user"

    !type gist-commit:
        change_status <: gist-commit_change_status:
            @json_tag = "change_status"
        committed_at <: datetime:
            @json_tag = "committed_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_gist-commit_user:
            @json_tag = "user"
        version <: string:
            @json_tag = "version"

    !type gist-commit_change_status:
        additions <: int?:
            @json_tag = "additions"
        deletions <: int?:
            @json_tag = "deletions"
        total <: int?:
            @json_tag = "total"

    !type gist-simple:
        comments <: int?:
            @json_tag = "comments"
        comments_url <: string?:
            @json_tag = "comments_url"
        commits_url <: string?:
            @json_tag = "commits_url"
        created_at <: string?:
            @json_tag = "created_at"
        description <: string?:
            @json_tag = "description"
        files <: EXTERNAL_gist-simple_files?:
            @json_tag = "files"
        forks_url <: string?:
            @json_tag = "forks_url"
        git_pull_url <: string?:
            @json_tag = "git_pull_url"
        git_push_url <: string?:
            @json_tag = "git_push_url"
        html_url <: string?:
            @json_tag = "html_url"
        id <: string?:
            @json_tag = "id"
        node_id <: string?:
            @json_tag = "node_id"
        owner <: simple-user?:
            @json_tag = "owner"
        public <: bool?:
            @json_tag = "public"
        truncated <: bool?:
            @json_tag = "truncated"
        updated_at <: string?:
            @json_tag = "updated_at"
        url <: string?:
            @json_tag = "url"
        user <: string?:
            @json_tag = "user"

    !type git-commit:
        author <: git-commit_author?:
            @json_tag = "author"
        committer <: git-commit_committer?:
            @json_tag = "committer"
        html_url <: string?:
            @json_tag = "html_url"
        message <: string?:
            @json_tag = "message"
        node_id <: string?:
            @json_tag = "node_id"
        parents <: sequence of git-commit_parents?:
            @json_tag = "parents"
        sha <: string?:
            @json_tag = "sha"
        tree <: git-commit_tree?:
            @json_tag = "tree"
        url <: string?:
            @json_tag = "url"
        verification <: git-commit_verification?:
            @json_tag = "verification"

    !type git-commit_author:
        date_ <: datetime?:
            @json_tag = "date"
        email <: string:
            @json_tag = "email"
        name <: string:
            @json_tag = "name"

    !type git-commit_committer:
        date_ <: datetime?:
            @json_tag = "date"
        email <: string:
            @json_tag = "email"
        name <: string:
            @json_tag = "name"

    !type git-commit_parents:
        html_url <: string?:
            @json_tag = "html_url"
        sha <: string?:
            @json_tag = "sha"
        url <: string?:
            @json_tag = "url"

    !type git-commit_tree:
        sha <: string?:
            @json_tag = "sha"
        url <: string?:
            @json_tag = "url"

    !type git-commit_verification:
        payload <: string?:
            @json_tag = "payload"
        reason <: string?:
            @json_tag = "reason"
        signature <: string?:
            @json_tag = "signature"
        verified <: bool?:
            @json_tag = "verified"

    !type git-ref:
        node_id <: string?:
            @json_tag = "node_id"
        object <: git-ref_object?:
            @json_tag = "object"
        ref <: string?:
            @json_tag = "ref"
        url <: string?:
            @json_tag = "url"

    !type git-ref_object:
        sha <: string(40..40)?:
            @json_tag = "sha"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type git-tag:
        message <: string:
            @json_tag = "message"
        node_id <: string:
            @json_tag = "node_id"
        object <: git-tag_object:
            @json_tag = "object"
        sha <: string:
            @json_tag = "sha"
        tag <: string:
            @json_tag = "tag"
        tagger <: git-tag_tagger:
            @json_tag = "tagger"
        url <: string:
            @json_tag = "url"
        verification <: verification?:
            @json_tag = "verification"

    !type git-tag_object:
        sha <: string:
            @json_tag = "sha"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type git-tag_tagger:
        date_ <: string:
            @json_tag = "date"
        email <: string:
            @json_tag = "email"
        name <: string:
            @json_tag = "name"

    !type git-tree:
        sha <: string:
            @json_tag = "sha"
        tree <: sequence of git-tree_tree:
            @json_tag = "tree"
        truncated <: bool:
            @json_tag = "truncated"
        url <: string:
            @json_tag = "url"

    !type git-tree_tree:
        mode <: string?:
            @json_tag = "mode"
        path <: string?:
            @json_tag = "path"
        sha <: string?:
            @json_tag = "sha"
        size <: int?:
            @json_tag = "size"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type git-user:
        date_ <: string?:
            @json_tag = "date"
        email <: string?:
            @json_tag = "email"
        name <: string?:
            @json_tag = "name"

    !type gitignore-template:
        name <: string:
            @json_tag = "name"
        source <: string:
            @json_tag = "source"

    !type gpg-key:
        can_certify <: bool:
            @json_tag = "can_certify"
        can_encrypt_comms <: bool:
            @json_tag = "can_encrypt_comms"
        can_encrypt_storage <: bool:
            @json_tag = "can_encrypt_storage"
        can_sign <: bool:
            @json_tag = "can_sign"
        created_at <: datetime:
            @json_tag = "created_at"
        emails <: sequence of gpg-key_emails:
            @json_tag = "emails"
        expires_at <: datetime:
            @json_tag = "expires_at"
        id <: int:
            @json_tag = "id"
        key_id <: string:
            @json_tag = "key_id"
        primary_key_id <: int:
            @json_tag = "primary_key_id"
        public_key <: string:
            @json_tag = "public_key"
        raw_key <: string:
            @json_tag = "raw_key"
        subkeys <: sequence of gpg-key_subkeys:
            @json_tag = "subkeys"

    !type gpg-key_emails:
        email <: string?:
            @json_tag = "email"
        verified <: bool?:
            @json_tag = "verified"

    !type gpg-key_subkeys:
        can_certify <: bool?:
            @json_tag = "can_certify"
        can_encrypt_comms <: bool?:
            @json_tag = "can_encrypt_comms"
        can_encrypt_storage <: bool?:
            @json_tag = "can_encrypt_storage"
        can_sign <: bool?:
            @json_tag = "can_sign"
        created_at <: string?:
            @json_tag = "created_at"
        emails <: sequence of EXTERNAL_gpg-key_subkeys_emails?:
            @json_tag = "emails"
        expires_at <: string?:
            @json_tag = "expires_at"
        id <: int?:
            @json_tag = "id"
        key_id <: string?:
            @json_tag = "key_id"
        primary_key_id <: int?:
            @json_tag = "primary_key_id"
        public_key <: string?:
            @json_tag = "public_key"
        raw_key <: string?:
            @json_tag = "raw_key"
        subkeys <: sequence of EXTERNAL_gpg-key_subkeys_subkeys?:
            @json_tag = "subkeys"

    !type group-mapping:
        group_description <: string?:
            @json_tag = "group_description"
        group_id <: string?:
            @json_tag = "group_id"
        group_name <: string?:
            @json_tag = "group_name"
        groups <: sequence of group-mapping_groups?:
            @json_tag = "groups"
        status <: string?:
            @json_tag = "status"
        synced_at <: string?:
            @json_tag = "synced_at"

    !type group-mapping_groups:
        group_description <: string:
            @json_tag = "group_description"
        group_id <: string:
            @json_tag = "group_id"
        group_name <: string:
            @json_tag = "group_name"

    !type hook:
        active <: bool:
            @json_tag = "active"
        config <: hook_config:
            @json_tag = "config"
        created_at <: datetime:
            @json_tag = "created_at"
        events <: sequence of string:
            @json_tag = "events"
        id <: int:
            @json_tag = "id"
        last_response <: hook-response:
            @json_tag = "last_response"
        name <: string:
            @json_tag = "name"
        ping_url <: string:
            @json_tag = "ping_url"
        test_url <: string:
            @json_tag = "test_url"
        type <: string:
            @json_tag = "type"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type hook-response:
        code <: int:
            @json_tag = "code"
        message <: string:
            @json_tag = "message"
        status <: string:
            @json_tag = "status"

    !type hook_config:
        content_type <: string?:
            @json_tag = "content_type"
        digest <: string?:
            @json_tag = "digest"
        email <: string?:
            @json_tag = "email"
        insecure_ssl <: string?:
            @json_tag = "insecure_ssl"
        password <: string?:
            @json_tag = "password"
        room <: string?:
            @json_tag = "room"
        secret <: string?:
            @json_tag = "secret"
        subdomain <: string?:
            @json_tag = "subdomain"
        token <: string?:
            @json_tag = "token"
        url <: string?:
            @json_tag = "url"

    !type hovercard:
        contexts <: sequence of hovercard_contexts:
            @json_tag = "contexts"

    !type hovercard_contexts:
        message <: string:
            @json_tag = "message"
        octicon <: string:
            @json_tag = "octicon"

    !type import:
        authors_count <: int?:
            @json_tag = "authors_count"
        authors_url <: string:
            @json_tag = "authors_url"
        commit_count <: int?:
            @json_tag = "commit_count"
        error_message <: string?:
            @json_tag = "error_message"
        failed_step <: string?:
            @json_tag = "failed_step"
        has_large_files <: bool?:
            @json_tag = "has_large_files"
        html_url <: string:
            @json_tag = "html_url"
        import_percent <: int?:
            @json_tag = "import_percent"
        large_files_count <: int?:
            @json_tag = "large_files_count"
        large_files_size <: int?:
            @json_tag = "large_files_size"
        message <: string?:
            @json_tag = "message"
        project_choices <: sequence of import_project_choices?:
            @json_tag = "project_choices"
        push_percent <: int?:
            @json_tag = "push_percent"
        repository_url <: string:
            @json_tag = "repository_url"
        status <: string:
            @json_tag = "status"
        status_text <: string?:
            @json_tag = "status_text"
        svc_root <: string?:
            @json_tag = "svc_root"
        svn_root <: string?:
            @json_tag = "svn_root"
        tfvc_project <: string?:
            @json_tag = "tfvc_project"
        url <: string:
            @json_tag = "url"
        use_lfs <: string?:
            @json_tag = "use_lfs"
        vcs <: string:
            @json_tag = "vcs"
        vcs_url <: string:
            @json_tag = "vcs_url"

    !type import_project_choices:
        human_name <: string?:
            @json_tag = "human_name"
        tfvc_project <: string?:
            @json_tag = "tfvc_project"
        vcs <: string?:
            @json_tag = "vcs"

    !type installation:
        access_tokens_url <: string:
            @json_tag = "access_tokens_url"
        account <: EXTERNAL_installation_account:
            @json_tag = "account"
        app_id <: int:
            @json_tag = "app_id"
        app_slug <: string:
            @json_tag = "app_slug"
        contact_email <: string?:
            @json_tag = "contact_email"
        created_at <: datetime:
            @json_tag = "created_at"
        events <: sequence of string:
            @json_tag = "events"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        permissions <: installation_permissions:
            @json_tag = "permissions"
        repositories_url <: string:
            @json_tag = "repositories_url"
        repository_selection <: string:
            @json_tag = "repository_selection"
        single_file_name <: string:
            @json_tag = "single_file_name"
        suspended_at <: datetime?:
            @json_tag = "suspended_at"
        suspended_by <: EXTERNAL_installation_suspended_by?:
            @json_tag = "suspended_by"
        target_id <: int:
            @json_tag = "target_id"
        target_type <: string:
            @json_tag = "target_type"
        updated_at <: datetime:
            @json_tag = "updated_at"

    !type installation-token:
        expires_at <: string?:
            @json_tag = "expires_at"
        permissions <: installation-token_permissions?:
            @json_tag = "permissions"
        repositories <: sequence of repository?:
            @json_tag = "repositories"
        repository_selection <: string?:
            @json_tag = "repository_selection"
        single_file <: string?:
            @json_tag = "single_file"
        token <: string?:
            @json_tag = "token"

    !type installation-token_permissions:
        contents <: string?:
            @json_tag = "contents"
        issues <: string?:
            @json_tag = "issues"
        metadata <: string?:
            @json_tag = "metadata"
        single_file <: string?:
            @json_tag = "single_file"

    !type installation_permissions:
        checks <: string?:
            @json_tag = "checks"
        contents <: string?:
            @json_tag = "contents"
        deployments <: string?:
            @json_tag = "deployments"
        issues <: string?:
            @json_tag = "issues"
        metadata <: string?:
            @json_tag = "metadata"
        organization_administration <: string?:
            @json_tag = "organization_administration"
        pull_requests <: string?:
            @json_tag = "pull_requests"
        statuses <: string?:
            @json_tag = "statuses"

    !type _integration:
        client_id <: string?:
            @json_tag = "client_id"
        client_secret <: string?:
            @json_tag = "client_secret"
        created_at <: datetime:
            @json_tag = "created_at"
        description <: string:
            @json_tag = "description"
        events <: sequence of string:
            @json_tag = "events"
        external_url <: string:
            @json_tag = "external_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        installations_count <: int?:
            @json_tag = "installations_count"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        owner <: EXTERNAL_integration_owner:
            @json_tag = "owner"
        pem <: string?:
            @json_tag = "pem"
        permissions <: _integration_permissions:
            @json_tag = "permissions"
        slug <: string?:
            @json_tag = "slug"
        updated_at <: datetime:
            @json_tag = "updated_at"
        webhook_secret <: string?:
            @json_tag = "webhook_secret"

    !type _integration_permissions:
        checks <: string?:
            @json_tag = "checks"
        contents <: string?:
            @json_tag = "contents"
        deployments <: string?:
            @json_tag = "deployments"
        issues <: string?:
            @json_tag = "issues"
        metadata <: string?:
            @json_tag = "metadata"

    !type _interaction-limit:
        expires_at <: datetime:
            @json_tag = "expires_at"
        limit <: string:
            @json_tag = "limit"
        origin <: string:
            @json_tag = "origin"

    !type issue:
        active_lock_reason <: string?:
            @json_tag = "active_lock_reason"
        assignee <: EXTERNAL_issue_assignee:
            @json_tag = "assignee"
        assignees <: sequence of simple-user?:
            @json_tag = "assignees"
        author_association <: string:
            @json_tag = "author_association"
        body <: string?:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        closed_at <: datetime:
            @json_tag = "closed_at"
        closed_by <: EXTERNAL_issue_closed_by?:
            @json_tag = "closed_by"
        comments <: int:
            @json_tag = "comments"
        comments_url <: string:
            @json_tag = "comments_url"
        created_at <: datetime:
            @json_tag = "created_at"
        events_url <: string:
            @json_tag = "events_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        labels <: sequence of issue_labels:
            @json_tag = "labels"
        labels_url <: string:
            @json_tag = "labels_url"
        locked <: bool:
            @json_tag = "locked"
        milestone <: EXTERNAL_issue_milestone:
            @json_tag = "milestone"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        performed_via_github_app <: EXTERNAL_issue_performed_via_github_app?:
            @json_tag = "performed_via_github_app"
        pull_request <: issue_pull_request?:
            @json_tag = "pull_request"
        reactions <: reaction-rollup?:
            @json_tag = "reactions"
        repository <: repository?:
            @json_tag = "repository"
        repository_url <: string:
            @json_tag = "repository_url"
        state <: string:
            @json_tag = "state"
        timeline_url <: string?:
            @json_tag = "timeline_url"
        title <: string:
            @json_tag = "title"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_issue_user:
            @json_tag = "user"

    !type issue-comment:
        author_association <: string:
            @json_tag = "author_association"
        body <: string?:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        created_at <: datetime:
            @json_tag = "created_at"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        issue_url <: string:
            @json_tag = "issue_url"
        node_id <: string:
            @json_tag = "node_id"
        performed_via_github_app <: EXTERNAL_issue-comment_performed_via_github_app?:
            @json_tag = "performed_via_github_app"
        reactions <: reaction-rollup?:
            @json_tag = "reactions"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_issue-comment_user:
            @json_tag = "user"

    !type issue-event:
        actor <: EXTERNAL_issue-event_actor:
            @json_tag = "actor"
        assignee <: EXTERNAL_issue-event_assignee?:
            @json_tag = "assignee"
        assigner <: EXTERNAL_issue-event_assigner?:
            @json_tag = "assigner"
        author_association <: string?:
            @json_tag = "author_association"
        commit_id <: string:
            @json_tag = "commit_id"
        commit_url <: string:
            @json_tag = "commit_url"
        created_at <: datetime:
            @json_tag = "created_at"
        dismissed_review <: issue-event-dismissed-review?:
            @json_tag = "dismissed_review"
        event <: string:
            @json_tag = "event"
        id <: int:
            @json_tag = "id"
        issue <: issue-simple?:
            @json_tag = "issue"
        label <: issue-event-label?:
            @json_tag = "label"
        lock_reason <: string?:
            @json_tag = "lock_reason"
        milestone <: issue-event-milestone?:
            @json_tag = "milestone"
        node_id <: string:
            @json_tag = "node_id"
        project_card <: issue-event-project-card?:
            @json_tag = "project_card"
        rename <: issue-event-rename?:
            @json_tag = "rename"
        requested_reviewer <: EXTERNAL_issue-event_requested_reviewer?:
            @json_tag = "requested_reviewer"
        requested_team <: team?:
            @json_tag = "requested_team"
        review_requester <: EXTERNAL_issue-event_review_requester?:
            @json_tag = "review_requester"
        url <: string:
            @json_tag = "url"

    !type issue-event-dismissed-review:
        dismissal_commit_id <: string?:
            @json_tag = "dismissal_commit_id"
        dismissal_message <: string:
            @json_tag = "dismissal_message"
        review_id <: int:
            @json_tag = "review_id"
        state <: string:
            @json_tag = "state"

    !type issue-event-for-issue:
        actor <: simple-user?:
            @json_tag = "actor"
        author_association <: string?:
            @json_tag = "author_association"
        body <: string?:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        commit_id <: string?:
            @json_tag = "commit_id"
        commit_url <: string?:
            @json_tag = "commit_url"
        created_at <: string?:
            @json_tag = "created_at"
        event <: string?:
            @json_tag = "event"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        issue_url <: string?:
            @json_tag = "issue_url"
        lock_reason <: string?:
            @json_tag = "lock_reason"
        message <: string?:
            @json_tag = "message"
        node_id <: string?:
            @json_tag = "node_id"
        pull_request_url <: string?:
            @json_tag = "pull_request_url"
        sha <: string?:
            @json_tag = "sha"
        state <: string?:
            @json_tag = "state"
        submitted_at <: string?:
            @json_tag = "submitted_at"
        updated_at <: string?:
            @json_tag = "updated_at"
        url <: string?:
            @json_tag = "url"

    !type issue-event-label:
        color <: string:
            @json_tag = "color"
        name <: string:
            @json_tag = "name"

    !type issue-event-milestone:
        title <: string:
            @json_tag = "title"

    !type issue-event-project-card:
        column_name <: string:
            @json_tag = "column_name"
        id <: int:
            @json_tag = "id"
        previous_column_name <: string?:
            @json_tag = "previous_column_name"
        project_id <: int:
            @json_tag = "project_id"
        project_url <: string:
            @json_tag = "project_url"
        url <: string:
            @json_tag = "url"

    !type issue-event-rename:
        from <: string:
            @json_tag = "from"
        to <: string:
            @json_tag = "to"

    !type issue-search-result-item:
        active_lock_reason <: string?:
            @json_tag = "active_lock_reason"
        assignee <: EXTERNAL_issue-search-result-item_assignee:
            @json_tag = "assignee"
        assignees <: sequence of simple-user?:
            @json_tag = "assignees"
        author_association <: string:
            @json_tag = "author_association"
        body <: string?:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        closed_at <: datetime:
            @json_tag = "closed_at"
        comments <: int:
            @json_tag = "comments"
        comments_url <: string:
            @json_tag = "comments_url"
        created_at <: datetime:
            @json_tag = "created_at"
        draft <: bool?:
            @json_tag = "draft"
        events_url <: string:
            @json_tag = "events_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        labels <: sequence of issue-search-result-item_labels:
            @json_tag = "labels"
        labels_url <: string:
            @json_tag = "labels_url"
        locked <: bool:
            @json_tag = "locked"
        milestone <: EXTERNAL_issue-search-result-item_milestone:
            @json_tag = "milestone"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        performed_via_github_app <: EXTERNAL_issue-search-result-item_performed_via_github_app?:
            @json_tag = "performed_via_github_app"
        pull_request <: issue-search-result-item_pull_request?:
            @json_tag = "pull_request"
        repository <: repository?:
            @json_tag = "repository"
        repository_url <: string:
            @json_tag = "repository_url"
        score <: int:
            @json_tag = "score"
        state <: string:
            @json_tag = "state"
        text_matches <: search-result-text-matches?:
            @json_tag = "text_matches"
        timeline_url <: string?:
            @json_tag = "timeline_url"
        title <: string:
            @json_tag = "title"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_issue-search-result-item_user:
            @json_tag = "user"

    !type issue-search-result-item_labels:
        color <: string?:
            @json_tag = "color"
        default <: bool?:
            @json_tag = "default"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        url <: string?:
            @json_tag = "url"

    !type issue-search-result-item_pull_request:
        diff_url <: string:
            @json_tag = "diff_url"
        html_url <: string:
            @json_tag = "html_url"
        merged_at <: datetime?:
            @json_tag = "merged_at"
        patch_url <: string:
            @json_tag = "patch_url"
        url <: string:
            @json_tag = "url"

    !type issue-simple:
        active_lock_reason <: string?:
            @json_tag = "active_lock_reason"
        assignee <: EXTERNAL_issue-simple_assignee:
            @json_tag = "assignee"
        assignees <: sequence of simple-user?:
            @json_tag = "assignees"
        author_association <: string:
            @json_tag = "author_association"
        body <: string?:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        closed_at <: datetime:
            @json_tag = "closed_at"
        comments <: int:
            @json_tag = "comments"
        comments_url <: string:
            @json_tag = "comments_url"
        created_at <: datetime:
            @json_tag = "created_at"
        events_url <: string:
            @json_tag = "events_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        labels <: sequence of issue-simple_labels:
            @json_tag = "labels"
        labels_url <: string:
            @json_tag = "labels_url"
        locked <: bool:
            @json_tag = "locked"
        milestone <: EXTERNAL_issue-simple_milestone:
            @json_tag = "milestone"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        performed_via_github_app <: EXTERNAL_issue-simple_performed_via_github_app?:
            @json_tag = "performed_via_github_app"
        pull_request <: issue-simple_pull_request?:
            @json_tag = "pull_request"
        repository <: repository?:
            @json_tag = "repository"
        repository_url <: string:
            @json_tag = "repository_url"
        state <: string:
            @json_tag = "state"
        timeline_url <: string?:
            @json_tag = "timeline_url"
        title <: string:
            @json_tag = "title"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_issue-simple_user:
            @json_tag = "user"

    !type issue-simple_labels:
        color <: string?:
            @json_tag = "color"
        default <: bool?:
            @json_tag = "default"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        url <: string?:
            @json_tag = "url"

    !type issue-simple_pull_request:
        diff_url <: string:
            @json_tag = "diff_url"
        html_url <: string:
            @json_tag = "html_url"
        merged_at <: datetime?:
            @json_tag = "merged_at"
        patch_url <: string:
            @json_tag = "patch_url"
        url <: string:
            @json_tag = "url"

    !union issue_labels:
        str
        object

    !type issue_labels_object:
        color <: string?:
            @json_tag = "color"
        default <: bool?:
            @json_tag = "default"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        url <: string?:
            @json_tag = "url"

    !type issue_pull_request:
        diff_url <: string:
            @json_tag = "diff_url"
        html_url <: string:
            @json_tag = "html_url"
        merged_at <: datetime?:
            @json_tag = "merged_at"
        patch_url <: string:
            @json_tag = "patch_url"
        url <: string:
            @json_tag = "url"

    !type job:
        check_run_url <: string:
            @json_tag = "check_run_url"
        completed_at <: datetime:
            @json_tag = "completed_at"
        conclusion <: string:
            @json_tag = "conclusion"
        head_sha <: string:
            @json_tag = "head_sha"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        run_id <: int:
            @json_tag = "run_id"
        run_url <: string:
            @json_tag = "run_url"
        started_at <: datetime:
            @json_tag = "started_at"
        status <: string:
            @json_tag = "status"
        steps <: sequence of job_steps?:
            @json_tag = "steps"
        url <: string:
            @json_tag = "url"

    !type job_steps:
        completed_at <: datetime?:
            @json_tag = "completed_at"
        conclusion <: string:
            @json_tag = "conclusion"
        name <: string:
            @json_tag = "name"
        number <: int:
            @json_tag = "number"
        started_at <: datetime?:
            @json_tag = "started_at"
        status <: string:
            @json_tag = "status"

    !type key:
        created_at <: datetime?:
            @json_tag = "created_at"
        id <: int?:
            @json_tag = "id"
        key <: string?:
            @json_tag = "key"
        key_id <: string?:
            @json_tag = "key_id"
        read_only <: bool?:
            @json_tag = "read_only"
        title <: string?:
            @json_tag = "title"
        url <: string?:
            @json_tag = "url"
        verified <: bool?:
            @json_tag = "verified"

    !type key-simple:
        id <: int:
            @json_tag = "id"
        key <: string:
            @json_tag = "key"

    !type label:
        color <: string:
            @json_tag = "color"
        default <: bool:
            @json_tag = "default"
        description <: string?:
            @json_tag = "description"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        url <: string:
            @json_tag = "url"

    !type label-search-result-item:
        color <: string:
            @json_tag = "color"
        default <: bool:
            @json_tag = "default"
        description <: string:
            @json_tag = "description"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        score <: int:
            @json_tag = "score"
        text_matches <: search-result-text-matches?:
            @json_tag = "text_matches"
        url <: string:
            @json_tag = "url"

    !type license:
        body <: string:
            @json_tag = "body"
        conditions <: sequence of string:
            @json_tag = "conditions"
        description <: string:
            @json_tag = "description"
        featured <: bool:
            @json_tag = "featured"
        html_url <: string:
            @json_tag = "html_url"
        implementation <: string:
            @json_tag = "implementation"
        key <: string:
            @json_tag = "key"
        limitations <: sequence of string:
            @json_tag = "limitations"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        permissions <: sequence of string:
            @json_tag = "permissions"
        spdx_id <: string:
            @json_tag = "spdx_id"
        url <: string:
            @json_tag = "url"

    !type license-content:
        _links <: license-content__links:
            @json_tag = "_links"
        content <: string:
            @json_tag = "content"
        download_url <: string:
            @json_tag = "download_url"
        encoding <: string:
            @json_tag = "encoding"
        git_url <: string:
            @json_tag = "git_url"
        html_url <: string:
            @json_tag = "html_url"
        license <: EXTERNAL_license-content_license:
            @json_tag = "license"
        name <: string:
            @json_tag = "name"
        path <: string:
            @json_tag = "path"
        sha <: string:
            @json_tag = "sha"
        size <: int:
            @json_tag = "size"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type license-content__links:
        git <: string:
            @json_tag = "git"
        html <: string:
            @json_tag = "html"
        self <: string:
            @json_tag = "self"

    !type license-simple:
        html_url <: string?:
            @json_tag = "html_url"
        key <: string:
            @json_tag = "key"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        spdx_id <: string:
            @json_tag = "spdx_id"
        url <: string:
            @json_tag = "url"

    !type link:
        href <: string:
            @json_tag = "href"

    !type link-with-type:
        href <: string:
            @json_tag = "href"
        type <: string:
            @json_tag = "type"

    !type marketplace-account:
        email <: string?:
            @json_tag = "email"
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        node_id <: string?:
            @json_tag = "node_id"
        organization_billing_email <: string?:
            @json_tag = "organization_billing_email"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type marketplace-listing-plan:
        accounts_url <: string:
            @json_tag = "accounts_url"
        bullets <: sequence of string:
            @json_tag = "bullets"
        description <: string:
            @json_tag = "description"
        has_free_trial <: bool:
            @json_tag = "has_free_trial"
        id <: int:
            @json_tag = "id"
        monthly_price_in_cents <: int:
            @json_tag = "monthly_price_in_cents"
        name <: string:
            @json_tag = "name"
        number <: int:
            @json_tag = "number"
        price_model <: string:
            @json_tag = "price_model"
        state <: string:
            @json_tag = "state"
        unit_name <: string:
            @json_tag = "unit_name"
        url <: string:
            @json_tag = "url"
        yearly_price_in_cents <: int:
            @json_tag = "yearly_price_in_cents"

    !type marketplace-purchase:
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        marketplace_pending_change <: marketplace-purchase_marketplace_pending_change?:
            @json_tag = "marketplace_pending_change"
        marketplace_purchase <: marketplace-purchase_marketplace_purchase:
            @json_tag = "marketplace_purchase"
        organization_billing_email <: string?:
            @json_tag = "organization_billing_email"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type marketplace-purchase_marketplace_pending_change:
        effective_date <: string?:
            @json_tag = "effective_date"
        id <: int?:
            @json_tag = "id"
        is_installed <: bool?:
            @json_tag = "is_installed"
        plan <: marketplace-listing-plan?:
            @json_tag = "plan"
        unit_count <: int?:
            @json_tag = "unit_count"

    !type marketplace-purchase_marketplace_purchase:
        billing_cycle <: string?:
            @json_tag = "billing_cycle"
        free_trial_ends_on <: string?:
            @json_tag = "free_trial_ends_on"
        is_installed <: bool?:
            @json_tag = "is_installed"
        next_billing_date <: string?:
            @json_tag = "next_billing_date"
        on_free_trial <: bool?:
            @json_tag = "on_free_trial"
        plan <: marketplace-listing-plan?:
            @json_tag = "plan"
        unit_count <: int?:
            @json_tag = "unit_count"
        updated_at <: string?:
            @json_tag = "updated_at"

    !type migration:
        archive_url <: string?:
            @json_tag = "archive_url"
        created_at <: datetime:
            @json_tag = "created_at"
        exclude <: sequence of EXTERNAL_migration_exclude?:
            @json_tag = "exclude"
        exclude_attachments <: bool:
            @json_tag = "exclude_attachments"
        guid <: string:
            @json_tag = "guid"
        id <: int:
            @json_tag = "id"
        lock_repositories <: bool:
            @json_tag = "lock_repositories"
        node_id <: string:
            @json_tag = "node_id"
        owner <: EXTERNAL_migration_owner:
            @json_tag = "owner"
        repositories <: sequence of repository:
            @json_tag = "repositories"
        state <: string:
            @json_tag = "state"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type milestone:
        closed_at <: datetime:
            @json_tag = "closed_at"
        closed_issues <: int:
            @json_tag = "closed_issues"
        created_at <: datetime:
            @json_tag = "created_at"
        creator <: EXTERNAL_milestone_creator:
            @json_tag = "creator"
        description <: string:
            @json_tag = "description"
        due_on <: datetime:
            @json_tag = "due_on"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        labels_url <: string:
            @json_tag = "labels_url"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        open_issues <: int:
            @json_tag = "open_issues"
        state <: string:
            @json_tag = "state"
        title <: string:
            @json_tag = "title"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type minimal-repository:
        archive_url <: string:
            @json_tag = "archive_url"
        archived <: bool?:
            @json_tag = "archived"
        assignees_url <: string:
            @json_tag = "assignees_url"
        blobs_url <: string:
            @json_tag = "blobs_url"
        branches_url <: string:
            @json_tag = "branches_url"
        clone_url <: string?:
            @json_tag = "clone_url"
        collaborators_url <: string:
            @json_tag = "collaborators_url"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        compare_url <: string:
            @json_tag = "compare_url"
        contents_url <: string:
            @json_tag = "contents_url"
        contributors_url <: string:
            @json_tag = "contributors_url"
        created_at <: datetime?:
            @json_tag = "created_at"
        default_branch <: string?:
            @json_tag = "default_branch"
        delete_branch_on_merge <: bool?:
            @json_tag = "delete_branch_on_merge"
        deployments_url <: string:
            @json_tag = "deployments_url"
        description <: string:
            @json_tag = "description"
        disabled <: bool?:
            @json_tag = "disabled"
        downloads_url <: string:
            @json_tag = "downloads_url"
        events_url <: string:
            @json_tag = "events_url"
        fork <: bool:
            @json_tag = "fork"
        forks <: int?:
            @json_tag = "forks"
        forks_count <: int?:
            @json_tag = "forks_count"
        forks_url <: string:
            @json_tag = "forks_url"
        full_name <: string:
            @json_tag = "full_name"
        git_commits_url <: string:
            @json_tag = "git_commits_url"
        git_refs_url <: string:
            @json_tag = "git_refs_url"
        git_tags_url <: string:
            @json_tag = "git_tags_url"
        git_url <: string?:
            @json_tag = "git_url"
        has_downloads <: bool?:
            @json_tag = "has_downloads"
        has_issues <: bool?:
            @json_tag = "has_issues"
        has_pages <: bool?:
            @json_tag = "has_pages"
        has_projects <: bool?:
            @json_tag = "has_projects"
        has_wiki <: bool?:
            @json_tag = "has_wiki"
        homepage <: string?:
            @json_tag = "homepage"
        hooks_url <: string:
            @json_tag = "hooks_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        is_template <: bool?:
            @json_tag = "is_template"
        issue_comment_url <: string:
            @json_tag = "issue_comment_url"
        issue_events_url <: string:
            @json_tag = "issue_events_url"
        issues_url <: string:
            @json_tag = "issues_url"
        keys_url <: string:
            @json_tag = "keys_url"
        labels_url <: string:
            @json_tag = "labels_url"
        language <: string?:
            @json_tag = "language"
        languages_url <: string:
            @json_tag = "languages_url"
        license <: minimal-repository_license?:
            @json_tag = "license"
        merges_url <: string:
            @json_tag = "merges_url"
        milestones_url <: string:
            @json_tag = "milestones_url"
        mirror_url <: string?:
            @json_tag = "mirror_url"
        name <: string:
            @json_tag = "name"
        network_count <: int?:
            @json_tag = "network_count"
        node_id <: string:
            @json_tag = "node_id"
        notifications_url <: string:
            @json_tag = "notifications_url"
        open_issues <: int?:
            @json_tag = "open_issues"
        open_issues_count <: int?:
            @json_tag = "open_issues_count"
        owner <: EXTERNAL_minimal-repository_owner:
            @json_tag = "owner"
        permissions <: minimal-repository_permissions?:
            @json_tag = "permissions"
        private <: bool:
            @json_tag = "private"
        pulls_url <: string:
            @json_tag = "pulls_url"
        pushed_at <: datetime?:
            @json_tag = "pushed_at"
        releases_url <: string:
            @json_tag = "releases_url"
        size <: int?:
            @json_tag = "size"
        ssh_url <: string?:
            @json_tag = "ssh_url"
        stargazers_count <: int?:
            @json_tag = "stargazers_count"
        stargazers_url <: string:
            @json_tag = "stargazers_url"
        statuses_url <: string:
            @json_tag = "statuses_url"
        subscribers_count <: int?:
            @json_tag = "subscribers_count"
        subscribers_url <: string:
            @json_tag = "subscribers_url"
        subscription_url <: string:
            @json_tag = "subscription_url"
        svn_url <: string?:
            @json_tag = "svn_url"
        tags_url <: string:
            @json_tag = "tags_url"
        teams_url <: string:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        template_repository <: string?:
            @json_tag = "template_repository"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string:
            @json_tag = "trees_url"
        updated_at <: datetime?:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        visibility <: string?:
            @json_tag = "visibility"
        watchers <: int?:
            @json_tag = "watchers"
        watchers_count <: int?:
            @json_tag = "watchers_count"

    !type minimal-repository_license:
        key <: string?:
            @json_tag = "key"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        spdx_id <: string?:
            @json_tag = "spdx_id"
        url <: string?:
            @json_tag = "url"

    !type minimal-repository_permissions:
        admin <: bool?:
            @json_tag = "admin"
        pull <: bool?:
            @json_tag = "pull"
        push <: bool?:
            @json_tag = "push"

    !type object:
        assignee <: string?:
            @json_tag = "assignee"
        assignees <: sequence of string?:
            @json_tag = "assignees"
        body <: string?:
            @json_tag = "body"
        labels <: sequence of object_labels?:
            @json_tag = "labels"
        milestone <: int?:
            @json_tag = "milestone"
        title <: string:
            @json_tag = "title"

    !type object:
        code <: string?:
            @json_tag = "code"
        documentation_url <: string?:
            @json_tag = "documentation_url"
        message <: string?:
            @json_tag = "message"

    !type object:
        documentation_url <: string:
            @json_tag = "documentation_url"
        message <: string:
            @json_tag = "message"

    !type object:
        documentation_url <: string:
            @json_tag = "documentation_url"
        message <: string:
            @json_tag = "message"

    !type object:
        documentation_url <: string:
            @json_tag = "documentation_url"
        message <: string:
            @json_tag = "message"

    !type object:
        documentation_url <: string:
            @json_tag = "documentation_url"
        message <: string:
            @json_tag = "message"

    !type object:
        code <: string?:
            @json_tag = "code"
        documentation_url <: string?:
            @json_tag = "documentation_url"
        message <: string?:
            @json_tag = "message"

    !type object:
        documentation_url <: string:
            @json_tag = "documentation_url"
        message <: string:
            @json_tag = "message"

    !type objectRequest:
        assignee <: string?:
            @json_tag = "assignee"
        assignees <: sequence of string?:
            @json_tag = "assignees"
        body <: string?:
            @json_tag = "body"
        labels <: sequence of objectRequest_labels?:
            @json_tag = "labels"
        milestone <: int?:
            @json_tag = "milestone"
        title <: string:
            @json_tag = "title"

    !type objectRequest:
        content <: string:
            @json_tag = "content"

    !type objectRequest:
        assignees <: sequence of string?:
            @json_tag = "assignees"

    !type objectRequest:
        assignees <: sequence of string?:
            @json_tag = "assignees"

    !type objectRequest:
        content <: string:
            @json_tag = "content"

    !type objectRequest:
        key <: string:
            @json_tag = "key"
        read_only <: bool?:
            @json_tag = "read_only"
        title <: string?:
            @json_tag = "title"

    !type objectRequest:
        body <: string:
            @json_tag = "body"

    !type objectRequest:
        color <: string?:
            @json_tag = "color"
        description <: string?:
            @json_tag = "description"
        new_name <: string?:
            @json_tag = "new_name"

    !type objectRequest:
        assignee <: string?:
            @json_tag = "assignee"
        assignees <: sequence of string?:
            @json_tag = "assignees"
        body <: string?:
            @json_tag = "body"
        labels <: sequence of objectRequest_labels?:
            @json_tag = "labels"
        milestone <: int?:
            @json_tag = "milestone"
        state <: string?:
            @json_tag = "state"
        title <: string?:
            @json_tag = "title"

    !type objectRequest:
        labels <: sequence of string?:
            @json_tag = "labels"

    !type objectRequest:
        labels <: sequence of string:
            @json_tag = "labels"

    !type objectRequest:
        lock_reason <: string?:
            @json_tag = "lock_reason"

    !type objectRequest:
        color <: string?:
            @json_tag = "color"
        description <: string?:
            @json_tag = "description"
        name <: string:
            @json_tag = "name"

    !type objectRequest:
        body <: string:
            @json_tag = "body"

    !union objectRequest_labels:
        str
        object

    !type objectRequest_labels_object:
        color <: string?:
            @json_tag = "color"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"

    !type objectRequest_labels_object:
        color <: string?:
            @json_tag = "color"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"

    !union object_labels:
        str
        object

    !type object_labels_object:
        color <: string?:
            @json_tag = "color"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"

    !type org-hook:
        active <: bool:
            @json_tag = "active"
        config <: org-hook_config:
            @json_tag = "config"
        created_at <: datetime:
            @json_tag = "created_at"
        events <: sequence of string:
            @json_tag = "events"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        ping_url <: string:
            @json_tag = "ping_url"
        type <: string:
            @json_tag = "type"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type org-hook_config:
        content_type <: string?:
            @json_tag = "content_type"
        insecure_ssl <: string?:
            @json_tag = "insecure_ssl"
        secret <: string?:
            @json_tag = "secret"
        url <: string?:
            @json_tag = "url"

    !type org-membership:
        organization <: organization-simple:
            @json_tag = "organization"
        organization_url <: string:
            @json_tag = "organization_url"
        permissions <: org-membership_permissions?:
            @json_tag = "permissions"
        role <: string:
            @json_tag = "role"
        state <: string:
            @json_tag = "state"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_org-membership_user:
            @json_tag = "user"

    !type org-membership_permissions:
        can_create_repository <: bool:
            @json_tag = "can_create_repository"

    !type organization-actions-secret:
        created_at <: datetime:
            @json_tag = "created_at"
        name <: string:
            @json_tag = "name"
        selected_repositories_url <: string?:
            @json_tag = "selected_repositories_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        visibility <: string:
            @json_tag = "visibility"

    !type organization-full:
        avatar_url <: string:
            @json_tag = "avatar_url"
        billing_email <: string?:
            @json_tag = "billing_email"
        blog <: string?:
            @json_tag = "blog"
        collaborators <: int?:
            @json_tag = "collaborators"
        company <: string?:
            @json_tag = "company"
        created_at <: datetime:
            @json_tag = "created_at"
        default_repository_permission <: string?:
            @json_tag = "default_repository_permission"
        description <: string:
            @json_tag = "description"
        disk_usage <: int?:
            @json_tag = "disk_usage"
        email <: string?:
            @json_tag = "email"
        events_url <: string:
            @json_tag = "events_url"
        followers <: int:
            @json_tag = "followers"
        following <: int:
            @json_tag = "following"
        has_organization_projects <: bool:
            @json_tag = "has_organization_projects"
        has_repository_projects <: bool:
            @json_tag = "has_repository_projects"
        hooks_url <: string:
            @json_tag = "hooks_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        is_verified <: bool?:
            @json_tag = "is_verified"
        issues_url <: string:
            @json_tag = "issues_url"
        location <: string?:
            @json_tag = "location"
        login <: string:
            @json_tag = "login"
        members_allowed_repository_creation_type <: string?:
            @json_tag = "members_allowed_repository_creation_type"
        members_can_create_internal_repositories <: bool?:
            @json_tag = "members_can_create_internal_repositories"
        members_can_create_pages <: bool?:
            @json_tag = "members_can_create_pages"
        members_can_create_private_repositories <: bool?:
            @json_tag = "members_can_create_private_repositories"
        members_can_create_public_repositories <: bool?:
            @json_tag = "members_can_create_public_repositories"
        members_can_create_repositories <: bool?:
            @json_tag = "members_can_create_repositories"
        members_url <: string:
            @json_tag = "members_url"
        name <: string?:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        owned_private_repos <: int?:
            @json_tag = "owned_private_repos"
        plan <: organization-full_plan?:
            @json_tag = "plan"
        private_gists <: int?:
            @json_tag = "private_gists"
        public_gists <: int:
            @json_tag = "public_gists"
        public_members_url <: string:
            @json_tag = "public_members_url"
        public_repos <: int:
            @json_tag = "public_repos"
        repos_url <: string:
            @json_tag = "repos_url"
        total_private_repos <: int?:
            @json_tag = "total_private_repos"
        twitter_username <: string?:
            @json_tag = "twitter_username"
        two_factor_requirement_enabled <: bool?:
            @json_tag = "two_factor_requirement_enabled"
        type <: string:
            @json_tag = "type"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type organization-full_plan:
        filled_seats <: int?:
            @json_tag = "filled_seats"
        name <: string:
            @json_tag = "name"
        private_repos <: int:
            @json_tag = "private_repos"
        seats <: int?:
            @json_tag = "seats"
        space <: int:
            @json_tag = "space"

    !type organization-invitation:
        created_at <: string?:
            @json_tag = "created_at"
        email <: string?:
            @json_tag = "email"
        id <: int?:
            @json_tag = "id"
        invitation_team_url <: string?:
            @json_tag = "invitation_team_url"
        invitation_teams_url <: string?:
            @json_tag = "invitation_teams_url"
        inviter <: simple-user?:
            @json_tag = "inviter"
        login <: string?:
            @json_tag = "login"
        node_id <: string?:
            @json_tag = "node_id"
        role <: string?:
            @json_tag = "role"
        team_count <: int?:
            @json_tag = "team_count"

    !type organization-simple:
        avatar_url <: string:
            @json_tag = "avatar_url"
        description <: string:
            @json_tag = "description"
        events_url <: string:
            @json_tag = "events_url"
        hooks_url <: string:
            @json_tag = "hooks_url"
        id <: int:
            @json_tag = "id"
        issues_url <: string:
            @json_tag = "issues_url"
        login <: string:
            @json_tag = "login"
        members_url <: string:
            @json_tag = "members_url"
        node_id <: string:
            @json_tag = "node_id"
        public_members_url <: string:
            @json_tag = "public_members_url"
        repos_url <: string:
            @json_tag = "repos_url"
        url <: string:
            @json_tag = "url"

    !type packages-billing-usage:
        included_gigabytes_bandwidth <: int?:
            @json_tag = "included_gigabytes_bandwidth"
        total_gigabytes_bandwidth_used <: int?:
            @json_tag = "total_gigabytes_bandwidth_used"
        total_paid_gigabytes_bandwidth_used <: int?:
            @json_tag = "total_paid_gigabytes_bandwidth_used"

    !type page:
        cname <: string:
            @json_tag = "cname"
        custom_404 <: bool:
            @json_tag = "custom_404"
        html_url <: string?:
            @json_tag = "html_url"
        source <: pages-source-hash?:
            @json_tag = "source"
        status <: string:
            @json_tag = "status"
        url <: string:
            @json_tag = "url"

    !type page-build:
        commit <: string:
            @json_tag = "commit"
        created_at <: datetime:
            @json_tag = "created_at"
        duration <: int:
            @json_tag = "duration"
        error <: page-build_error:
            @json_tag = "error"
        pusher <: EXTERNAL_page-build_pusher:
            @json_tag = "pusher"
        status <: string:
            @json_tag = "status"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type page-build-status:
        status <: string:
            @json_tag = "status"
        url <: string:
            @json_tag = "url"

    !type page-build_error:
        message <: string:
            @json_tag = "message"

    !type pages-source-hash:
        branch <: string:
            @json_tag = "branch"
        path <: string:
            @json_tag = "path"

    !type participation-stats:
        all <: sequence of int?:
            @json_tag = "all"
        owner <: sequence of int?:
            @json_tag = "owner"

    !type porter-author:
        email <: string:
            @json_tag = "email"
        id <: int:
            @json_tag = "id"
        import_url <: string:
            @json_tag = "import_url"
        name <: string:
            @json_tag = "name"
        remote_id <: string:
            @json_tag = "remote_id"
        remote_name <: string:
            @json_tag = "remote_name"
        url <: string:
            @json_tag = "url"

    !type porter-large-file:
        oid <: string:
            @json_tag = "oid"
        path <: string:
            @json_tag = "path"
        ref_name <: string:
            @json_tag = "ref_name"
        size <: int:
            @json_tag = "size"

    !type private-user:
        avatar_url <: string:
            @json_tag = "avatar_url"
        bio <: string:
            @json_tag = "bio"
        blog <: string:
            @json_tag = "blog"
        business_plus <: bool?:
            @json_tag = "business_plus"
        collaborators <: int:
            @json_tag = "collaborators"
        company <: string:
            @json_tag = "company"
        created_at <: datetime:
            @json_tag = "created_at"
        disk_usage <: int:
            @json_tag = "disk_usage"
        email <: string:
            @json_tag = "email"
        events_url <: string:
            @json_tag = "events_url"
        followers <: int:
            @json_tag = "followers"
        followers_url <: string:
            @json_tag = "followers_url"
        following <: int:
            @json_tag = "following"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        hireable <: bool:
            @json_tag = "hireable"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        ldap_dn <: string?:
            @json_tag = "ldap_dn"
        location <: string:
            @json_tag = "location"
        login <: string:
            @json_tag = "login"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        owned_private_repos <: int:
            @json_tag = "owned_private_repos"
        plan <: private-user_plan?:
            @json_tag = "plan"
        private_gists <: int:
            @json_tag = "private_gists"
        public_gists <: int:
            @json_tag = "public_gists"
        public_repos <: int:
            @json_tag = "public_repos"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        suspended_at <: datetime?:
            @json_tag = "suspended_at"
        total_private_repos <: int:
            @json_tag = "total_private_repos"
        twitter_username <: string?:
            @json_tag = "twitter_username"
        two_factor_authentication <: bool:
            @json_tag = "two_factor_authentication"
        type <: string:
            @json_tag = "type"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type private-user_plan:
        collaborators <: int:
            @json_tag = "collaborators"
        name <: string:
            @json_tag = "name"
        private_repos <: int:
            @json_tag = "private_repos"
        space <: int:
            @json_tag = "space"

    !type project:
        body <: string:
            @json_tag = "body"
        cards_url <: string?:
            @json_tag = "cards_url"
        columns_url <: string:
            @json_tag = "columns_url"
        created_at <: datetime:
            @json_tag = "created_at"
        creator <: EXTERNAL_project_creator:
            @json_tag = "creator"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        organization_permission <: string?:
            @json_tag = "organization_permission"
        owner_url <: string:
            @json_tag = "owner_url"
        permissions <: project_permissions?:
            @json_tag = "permissions"
        private <: bool?:
            @json_tag = "private"
        state <: string:
            @json_tag = "state"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type project-card:
        archived <: bool?:
            @json_tag = "archived"
        column_url <: string:
            @json_tag = "column_url"
        content_url <: string?:
            @json_tag = "content_url"
        created_at <: datetime:
            @json_tag = "created_at"
        creator <: EXTERNAL_project-card_creator:
            @json_tag = "creator"
        id <: int:
            @json_tag = "id"
        node_id <: string:
            @json_tag = "node_id"
        note <: string:
            @json_tag = "note"
        project_url <: string:
            @json_tag = "project_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type project-column:
        cards_url <: string:
            @json_tag = "cards_url"
        created_at <: datetime:
            @json_tag = "created_at"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        project_url <: string:
            @json_tag = "project_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type project_permissions:
        admin <: bool:
            @json_tag = "admin"
        read <: bool:
            @json_tag = "read"
        write <: bool:
            @json_tag = "write"

    !type protected-branch:
        allow_deletions <: protected-branch_allow_deletions?:
            @json_tag = "allow_deletions"
        allow_force_pushes <: protected-branch_allow_force_pushes?:
            @json_tag = "allow_force_pushes"
        enforce_admins <: protected-branch_enforce_admins?:
            @json_tag = "enforce_admins"
        required_linear_history <: protected-branch_required_linear_history?:
            @json_tag = "required_linear_history"
        required_pull_request_reviews <: protected-branch_required_pull_request_reviews?:
            @json_tag = "required_pull_request_reviews"
        required_signatures <: protected-branch_required_signatures?:
            @json_tag = "required_signatures"
        required_status_checks <: status-check-policy?:
            @json_tag = "required_status_checks"
        restrictions <: branch-restriction-policy?:
            @json_tag = "restrictions"
        url <: string:
            @json_tag = "url"

    !type protected-branch-admin-enforced:
        enabled <: bool:
            @json_tag = "enabled"
        url <: string:
            @json_tag = "url"

    !type protected-branch-pull-request-review:
        dismiss_stale_reviews <: bool:
            @json_tag = "dismiss_stale_reviews"
        dismissal_restrictions <: protected-branch-pull-request-review_dismissal_restrictions?:
            @json_tag = "dismissal_restrictions"
        require_code_owner_reviews <: bool:
            @json_tag = "require_code_owner_reviews"
        required_approving_review_count <: int?:
            @json_tag = "required_approving_review_count"
        url <: string?:
            @json_tag = "url"

    !type protected-branch-pull-request-review_dismissal_restrictions:
        teams <: sequence of team?:
            @json_tag = "teams"
        teams_url <: string?:
            @json_tag = "teams_url"
        url <: string?:
            @json_tag = "url"
        users <: sequence of simple-user?:
            @json_tag = "users"
        users_url <: string?:
            @json_tag = "users_url"

    !type protected-branch_allow_deletions:
        enabled <: bool:
            @json_tag = "enabled"

    !type protected-branch_allow_force_pushes:
        enabled <: bool:
            @json_tag = "enabled"

    !type protected-branch_enforce_admins:
        enabled <: bool:
            @json_tag = "enabled"
        url <: string:
            @json_tag = "url"

    !type protected-branch_required_linear_history:
        enabled <: bool:
            @json_tag = "enabled"

    !type protected-branch_required_pull_request_reviews:
        dismiss_stale_reviews <: bool?:
            @json_tag = "dismiss_stale_reviews"
        dismissal_restrictions <: protected-branch_required_pull_request_reviews_dismissal_restrictions?:
            @json_tag = "dismissal_restrictions"
        require_code_owner_reviews <: bool?:
            @json_tag = "require_code_owner_reviews"
        required_approving_review_count <: int?:
            @json_tag = "required_approving_review_count"
        url <: string:
            @json_tag = "url"

    !type protected-branch_required_pull_request_reviews_dismissal_restrictions:
        teams <: sequence of team:
            @json_tag = "teams"
        teams_url <: string:
            @json_tag = "teams_url"
        url <: string:
            @json_tag = "url"
        users <: sequence of simple-user:
            @json_tag = "users"
        users_url <: string:
            @json_tag = "users_url"

    !type protected-branch_required_signatures:
        enabled <: bool:
            @json_tag = "enabled"
        url <: string:
            @json_tag = "url"

    !type public-user:
        avatar_url <: string:
            @json_tag = "avatar_url"
        bio <: string:
            @json_tag = "bio"
        blog <: string:
            @json_tag = "blog"
        collaborators <: int?:
            @json_tag = "collaborators"
        company <: string:
            @json_tag = "company"
        created_at <: datetime:
            @json_tag = "created_at"
        disk_usage <: int?:
            @json_tag = "disk_usage"
        email <: string:
            @json_tag = "email"
        events_url <: string:
            @json_tag = "events_url"
        followers <: int:
            @json_tag = "followers"
        followers_url <: string:
            @json_tag = "followers_url"
        following <: int:
            @json_tag = "following"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        hireable <: bool:
            @json_tag = "hireable"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        location <: string:
            @json_tag = "location"
        login <: string:
            @json_tag = "login"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        owned_private_repos <: int?:
            @json_tag = "owned_private_repos"
        plan <: public-user_plan?:
            @json_tag = "plan"
        private_gists <: int?:
            @json_tag = "private_gists"
        public_gists <: int:
            @json_tag = "public_gists"
        public_repos <: int:
            @json_tag = "public_repos"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        suspended_at <: datetime?:
            @json_tag = "suspended_at"
        total_private_repos <: int?:
            @json_tag = "total_private_repos"
        twitter_username <: string?:
            @json_tag = "twitter_username"
        type <: string:
            @json_tag = "type"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type public-user_plan:
        collaborators <: int:
            @json_tag = "collaborators"
        name <: string:
            @json_tag = "name"
        private_repos <: int:
            @json_tag = "private_repos"
        space <: int:
            @json_tag = "space"

    !type pull-request:
        _links <: pull-request__links:
            @json_tag = "_links"
        active_lock_reason <: string?:
            @json_tag = "active_lock_reason"
        additions <: int:
            @json_tag = "additions"
        assignee <: EXTERNAL_pull-request_assignee:
            @json_tag = "assignee"
        assignees <: sequence of simple-user?:
            @json_tag = "assignees"
        author_association <: string:
            @json_tag = "author_association"
        base <: pull-request_base:
            @json_tag = "base"
        body <: string:
            @json_tag = "body"
        changed_files <: int:
            @json_tag = "changed_files"
        closed_at <: datetime:
            @json_tag = "closed_at"
        comments <: int:
            @json_tag = "comments"
        comments_url <: string:
            @json_tag = "comments_url"
        commits <: int:
            @json_tag = "commits"
        commits_url <: string:
            @json_tag = "commits_url"
        created_at <: datetime:
            @json_tag = "created_at"
        deletions <: int:
            @json_tag = "deletions"
        diff_url <: string:
            @json_tag = "diff_url"
        draft <: bool?:
            @json_tag = "draft"
        head <: pull-request_head:
            @json_tag = "head"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        issue_url <: string:
            @json_tag = "issue_url"
        labels <: sequence of pull-request_labels:
            @json_tag = "labels"
        locked <: bool:
            @json_tag = "locked"
        maintainer_can_modify <: bool:
            @json_tag = "maintainer_can_modify"
        merge_commit_sha <: string:
            @json_tag = "merge_commit_sha"
        mergeable <: bool:
            @json_tag = "mergeable"
        mergeable_state <: string:
            @json_tag = "mergeable_state"
        merged <: bool:
            @json_tag = "merged"
        merged_at <: datetime:
            @json_tag = "merged_at"
        merged_by <: EXTERNAL_pull-request_merged_by:
            @json_tag = "merged_by"
        milestone <: EXTERNAL_pull-request_milestone:
            @json_tag = "milestone"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        patch_url <: string:
            @json_tag = "patch_url"
        rebaseable <: bool?:
            @json_tag = "rebaseable"
        requested_reviewers <: sequence of simple-user?:
            @json_tag = "requested_reviewers"
        requested_teams <: sequence of team-simple?:
            @json_tag = "requested_teams"
        review_comment_url <: string:
            @json_tag = "review_comment_url"
        review_comments <: int:
            @json_tag = "review_comments"
        review_comments_url <: string:
            @json_tag = "review_comments_url"
        state <: string:
            @json_tag = "state"
        statuses_url <: string:
            @json_tag = "statuses_url"
        title <: string:
            @json_tag = "title"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_pull-request_user:
            @json_tag = "user"

    !type pull-request-merge-result:
        merged <: bool:
            @json_tag = "merged"
        message <: string:
            @json_tag = "message"
        sha <: string:
            @json_tag = "sha"

    !type pull-request-minimal:
        base <: pull-request-minimal_base:
            @json_tag = "base"
        head <: pull-request-minimal_head:
            @json_tag = "head"
        id <: int:
            @json_tag = "id"
        number <: int:
            @json_tag = "number"
        url <: string:
            @json_tag = "url"

    !type pull-request-minimal_base:
        ref <: string:
            @json_tag = "ref"
        repo <: pull-request-minimal_base_repo:
            @json_tag = "repo"
        sha <: string:
            @json_tag = "sha"

    !type pull-request-minimal_base_repo:
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        url <: string:
            @json_tag = "url"

    !type pull-request-minimal_head:
        ref <: string:
            @json_tag = "ref"
        repo <: pull-request-minimal_head_repo:
            @json_tag = "repo"
        sha <: string:
            @json_tag = "sha"

    !type pull-request-minimal_head_repo:
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        url <: string:
            @json_tag = "url"

    !type pull-request-review:
        _links <: pull-request-review__links:
            @json_tag = "_links"
        author_association <: string:
            @json_tag = "author_association"
        body <: string:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        commit_id <: string:
            @json_tag = "commit_id"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        node_id <: string:
            @json_tag = "node_id"
        pull_request_url <: string:
            @json_tag = "pull_request_url"
        state <: string:
            @json_tag = "state"
        submitted_at <: datetime?:
            @json_tag = "submitted_at"
        user <: EXTERNAL_pull-request-review_user:
            @json_tag = "user"

    !type pull-request-review-comment:
        _links <: pull-request-review-comment__links:
            @json_tag = "_links"
        author_association <: string:
            @json_tag = "author_association"
        body <: string:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        commit_id <: string:
            @json_tag = "commit_id"
        created_at <: datetime:
            @json_tag = "created_at"
        diff_hunk <: string:
            @json_tag = "diff_hunk"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        in_reply_to_id <: int?:
            @json_tag = "in_reply_to_id"
        line <: int?:
            @json_tag = "line"
        node_id <: string:
            @json_tag = "node_id"
        original_commit_id <: string:
            @json_tag = "original_commit_id"
        original_line <: int?:
            @json_tag = "original_line"
        original_position <: int:
            @json_tag = "original_position"
        original_start_line <: int?:
            @json_tag = "original_start_line"
        path <: string:
            @json_tag = "path"
        position <: int:
            @json_tag = "position"
        pull_request_review_id <: int:
            @json_tag = "pull_request_review_id"
        pull_request_url <: string:
            @json_tag = "pull_request_url"
        reactions <: reaction-rollup?:
            @json_tag = "reactions"
        side <: string?:
            @json_tag = "side"
        start_line <: int?:
            @json_tag = "start_line"
        start_side <: string?:
            @json_tag = "start_side"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: simple-user:
            @json_tag = "user"

    !type pull-request-review-comment__links:
        html <: pull-request-review-comment__links_html:
            @json_tag = "html"
        pull_request <: pull-request-review-comment__links_pull_request:
            @json_tag = "pull_request"
        self <: pull-request-review-comment__links_self:
            @json_tag = "self"

    !type pull-request-review-comment__links_html:
        href <: string:
            @json_tag = "href"

    !type pull-request-review-comment__links_pull_request:
        href <: string:
            @json_tag = "href"

    !type pull-request-review-comment__links_self:
        href <: string:
            @json_tag = "href"

    !type pull-request-review-request:
        teams <: sequence of pull-request-review-request_teams?:
            @json_tag = "teams"
        users <: sequence of pull-request-review-request_users?:
            @json_tag = "users"

    !type pull-request-review-request_teams:
        description <: string?:
            @json_tag = "description"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        members_url <: string?:
            @json_tag = "members_url"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        parent <: string?:
            @json_tag = "parent"
        permission <: string?:
            @json_tag = "permission"
        privacy <: string?:
            @json_tag = "privacy"
        repositories_url <: string?:
            @json_tag = "repositories_url"
        slug <: string?:
            @json_tag = "slug"
        url <: string?:
            @json_tag = "url"

    !type pull-request-review-request_users:
        avatar_url <: string?:
            @json_tag = "avatar_url"
        events_url <: string?:
            @json_tag = "events_url"
        followers_url <: string?:
            @json_tag = "followers_url"
        following_url <: string?:
            @json_tag = "following_url"
        gists_url <: string?:
            @json_tag = "gists_url"
        gravatar_id <: string?:
            @json_tag = "gravatar_id"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        login <: string?:
            @json_tag = "login"
        node_id <: string?:
            @json_tag = "node_id"
        organizations_url <: string?:
            @json_tag = "organizations_url"
        received_events_url <: string?:
            @json_tag = "received_events_url"
        repos_url <: string?:
            @json_tag = "repos_url"
        site_admin <: bool?:
            @json_tag = "site_admin"
        starred_url <: string?:
            @json_tag = "starred_url"
        subscriptions_url <: string?:
            @json_tag = "subscriptions_url"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type pull-request-review__links:
        html <: pull-request-review__links_html:
            @json_tag = "html"
        pull_request <: pull-request-review__links_pull_request:
            @json_tag = "pull_request"

    !type pull-request-review__links_html:
        href <: string:
            @json_tag = "href"

    !type pull-request-review__links_pull_request:
        href <: string:
            @json_tag = "href"

    !type pull-request-simple:
        _links <: pull-request-simple__links:
            @json_tag = "_links"
        active_lock_reason <: string?:
            @json_tag = "active_lock_reason"
        assignee <: EXTERNAL_pull-request-simple_assignee:
            @json_tag = "assignee"
        assignees <: sequence of simple-user?:
            @json_tag = "assignees"
        author_association <: string:
            @json_tag = "author_association"
        base <: pull-request-simple_base:
            @json_tag = "base"
        body <: string:
            @json_tag = "body"
        closed_at <: datetime:
            @json_tag = "closed_at"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        created_at <: datetime:
            @json_tag = "created_at"
        diff_url <: string:
            @json_tag = "diff_url"
        draft <: bool?:
            @json_tag = "draft"
        head <: pull-request-simple_head:
            @json_tag = "head"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        issue_url <: string:
            @json_tag = "issue_url"
        labels <: sequence of pull-request-simple_labels:
            @json_tag = "labels"
        locked <: bool:
            @json_tag = "locked"
        merge_commit_sha <: string:
            @json_tag = "merge_commit_sha"
        merged_at <: datetime:
            @json_tag = "merged_at"
        milestone <: EXTERNAL_pull-request-simple_milestone:
            @json_tag = "milestone"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        patch_url <: string:
            @json_tag = "patch_url"
        requested_reviewers <: sequence of simple-user?:
            @json_tag = "requested_reviewers"
        requested_teams <: sequence of team-simple?:
            @json_tag = "requested_teams"
        review_comment_url <: string:
            @json_tag = "review_comment_url"
        review_comments_url <: string:
            @json_tag = "review_comments_url"
        state <: string:
            @json_tag = "state"
        statuses_url <: string:
            @json_tag = "statuses_url"
        title <: string:
            @json_tag = "title"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_pull-request-simple_user:
            @json_tag = "user"

    !type pull-request-simple__links:
        comments <: link:
            @json_tag = "comments"
        commits <: link:
            @json_tag = "commits"
        html <: link:
            @json_tag = "html"
        issue <: link:
            @json_tag = "issue"
        review_comment <: link:
            @json_tag = "review_comment"
        review_comments <: link:
            @json_tag = "review_comments"
        self <: link:
            @json_tag = "self"
        statuses <: link:
            @json_tag = "statuses"

    !type pull-request-simple_base:
        label <: string:
            @json_tag = "label"
        ref <: string:
            @json_tag = "ref"
        repo <: repository:
            @json_tag = "repo"
        sha <: string:
            @json_tag = "sha"
        user <: EXTERNAL_pull-request-simple_base_user:
            @json_tag = "user"

    !type pull-request-simple_head:
        label <: string:
            @json_tag = "label"
        ref <: string:
            @json_tag = "ref"
        repo <: repository:
            @json_tag = "repo"
        sha <: string:
            @json_tag = "sha"
        user <: EXTERNAL_pull-request-simple_head_user:
            @json_tag = "user"

    !type pull-request-simple_labels:
        color <: string?:
            @json_tag = "color"
        default <: bool?:
            @json_tag = "default"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        url <: string?:
            @json_tag = "url"

    !type pull-request__links:
        comments <: link:
            @json_tag = "comments"
        commits <: link:
            @json_tag = "commits"
        html <: link:
            @json_tag = "html"
        issue <: link:
            @json_tag = "issue"
        review_comment <: link:
            @json_tag = "review_comment"
        review_comments <: link:
            @json_tag = "review_comments"
        self <: link:
            @json_tag = "self"
        statuses <: link:
            @json_tag = "statuses"

    !type pull-request_base:
        label <: string:
            @json_tag = "label"
        ref <: string:
            @json_tag = "ref"
        repo <: pull-request_base_repo:
            @json_tag = "repo"
        sha <: string:
            @json_tag = "sha"
        user <: pull-request_base_user:
            @json_tag = "user"

    !type pull-request_base_repo:
        allow_merge_commit <: bool?:
            @json_tag = "allow_merge_commit"
        allow_rebase_merge <: bool?:
            @json_tag = "allow_rebase_merge"
        allow_squash_merge <: bool?:
            @json_tag = "allow_squash_merge"
        archive_url <: string:
            @json_tag = "archive_url"
        archived <: bool:
            @json_tag = "archived"
        assignees_url <: string:
            @json_tag = "assignees_url"
        blobs_url <: string:
            @json_tag = "blobs_url"
        branches_url <: string:
            @json_tag = "branches_url"
        clone_url <: string:
            @json_tag = "clone_url"
        collaborators_url <: string:
            @json_tag = "collaborators_url"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        compare_url <: string:
            @json_tag = "compare_url"
        contents_url <: string:
            @json_tag = "contents_url"
        contributors_url <: string:
            @json_tag = "contributors_url"
        created_at <: datetime:
            @json_tag = "created_at"
        default_branch <: string:
            @json_tag = "default_branch"
        deployments_url <: string:
            @json_tag = "deployments_url"
        description <: string:
            @json_tag = "description"
        disabled <: bool:
            @json_tag = "disabled"
        downloads_url <: string:
            @json_tag = "downloads_url"
        events_url <: string:
            @json_tag = "events_url"
        fork <: bool:
            @json_tag = "fork"
        forks <: int:
            @json_tag = "forks"
        forks_count <: int:
            @json_tag = "forks_count"
        forks_url <: string:
            @json_tag = "forks_url"
        full_name <: string:
            @json_tag = "full_name"
        git_commits_url <: string:
            @json_tag = "git_commits_url"
        git_refs_url <: string:
            @json_tag = "git_refs_url"
        git_tags_url <: string:
            @json_tag = "git_tags_url"
        git_url <: string:
            @json_tag = "git_url"
        has_downloads <: bool:
            @json_tag = "has_downloads"
        has_issues <: bool:
            @json_tag = "has_issues"
        has_pages <: bool:
            @json_tag = "has_pages"
        has_projects <: bool:
            @json_tag = "has_projects"
        has_wiki <: bool:
            @json_tag = "has_wiki"
        homepage <: string:
            @json_tag = "homepage"
        hooks_url <: string:
            @json_tag = "hooks_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        issue_comment_url <: string:
            @json_tag = "issue_comment_url"
        issue_events_url <: string:
            @json_tag = "issue_events_url"
        issues_url <: string:
            @json_tag = "issues_url"
        keys_url <: string:
            @json_tag = "keys_url"
        labels_url <: string:
            @json_tag = "labels_url"
        language <: string:
            @json_tag = "language"
        languages_url <: string:
            @json_tag = "languages_url"
        license <: EXTERNAL_pull-request_base_repo_license:
            @json_tag = "license"
        master_branch <: string?:
            @json_tag = "master_branch"
        merges_url <: string:
            @json_tag = "merges_url"
        milestones_url <: string:
            @json_tag = "milestones_url"
        mirror_url <: string:
            @json_tag = "mirror_url"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        notifications_url <: string:
            @json_tag = "notifications_url"
        open_issues <: int:
            @json_tag = "open_issues"
        open_issues_count <: int:
            @json_tag = "open_issues_count"
        owner <: pull-request_base_repo_owner:
            @json_tag = "owner"
        permissions <: pull-request_base_repo_permissions?:
            @json_tag = "permissions"
        private <: bool:
            @json_tag = "private"
        pulls_url <: string:
            @json_tag = "pulls_url"
        pushed_at <: datetime:
            @json_tag = "pushed_at"
        releases_url <: string:
            @json_tag = "releases_url"
        size <: int:
            @json_tag = "size"
        ssh_url <: string:
            @json_tag = "ssh_url"
        stargazers_count <: int:
            @json_tag = "stargazers_count"
        stargazers_url <: string:
            @json_tag = "stargazers_url"
        statuses_url <: string:
            @json_tag = "statuses_url"
        subscribers_url <: string:
            @json_tag = "subscribers_url"
        subscription_url <: string:
            @json_tag = "subscription_url"
        svn_url <: string:
            @json_tag = "svn_url"
        tags_url <: string:
            @json_tag = "tags_url"
        teams_url <: string:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string:
            @json_tag = "trees_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        watchers <: int:
            @json_tag = "watchers"
        watchers_count <: int:
            @json_tag = "watchers_count"

    !type pull-request_base_repo_owner:
        avatar_url <: string:
            @json_tag = "avatar_url"
        events_url <: string:
            @json_tag = "events_url"
        followers_url <: string:
            @json_tag = "followers_url"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type pull-request_base_repo_permissions:
        admin <: bool:
            @json_tag = "admin"
        pull <: bool:
            @json_tag = "pull"
        push <: bool:
            @json_tag = "push"

    !type pull-request_base_user:
        avatar_url <: string:
            @json_tag = "avatar_url"
        events_url <: string:
            @json_tag = "events_url"
        followers_url <: string:
            @json_tag = "followers_url"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type pull-request_head:
        label <: string:
            @json_tag = "label"
        ref <: string:
            @json_tag = "ref"
        repo <: pull-request_head_repo:
            @json_tag = "repo"
        sha <: string:
            @json_tag = "sha"
        user <: pull-request_head_user:
            @json_tag = "user"

    !type pull-request_head_repo:
        allow_merge_commit <: bool?:
            @json_tag = "allow_merge_commit"
        allow_rebase_merge <: bool?:
            @json_tag = "allow_rebase_merge"
        allow_squash_merge <: bool?:
            @json_tag = "allow_squash_merge"
        archive_url <: string:
            @json_tag = "archive_url"
        archived <: bool:
            @json_tag = "archived"
        assignees_url <: string:
            @json_tag = "assignees_url"
        blobs_url <: string:
            @json_tag = "blobs_url"
        branches_url <: string:
            @json_tag = "branches_url"
        clone_url <: string:
            @json_tag = "clone_url"
        collaborators_url <: string:
            @json_tag = "collaborators_url"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        compare_url <: string:
            @json_tag = "compare_url"
        contents_url <: string:
            @json_tag = "contents_url"
        contributors_url <: string:
            @json_tag = "contributors_url"
        created_at <: datetime:
            @json_tag = "created_at"
        default_branch <: string:
            @json_tag = "default_branch"
        deployments_url <: string:
            @json_tag = "deployments_url"
        description <: string:
            @json_tag = "description"
        disabled <: bool:
            @json_tag = "disabled"
        downloads_url <: string:
            @json_tag = "downloads_url"
        events_url <: string:
            @json_tag = "events_url"
        fork <: bool:
            @json_tag = "fork"
        forks <: int:
            @json_tag = "forks"
        forks_count <: int:
            @json_tag = "forks_count"
        forks_url <: string:
            @json_tag = "forks_url"
        full_name <: string:
            @json_tag = "full_name"
        git_commits_url <: string:
            @json_tag = "git_commits_url"
        git_refs_url <: string:
            @json_tag = "git_refs_url"
        git_tags_url <: string:
            @json_tag = "git_tags_url"
        git_url <: string:
            @json_tag = "git_url"
        has_downloads <: bool:
            @json_tag = "has_downloads"
        has_issues <: bool:
            @json_tag = "has_issues"
        has_pages <: bool:
            @json_tag = "has_pages"
        has_projects <: bool:
            @json_tag = "has_projects"
        has_wiki <: bool:
            @json_tag = "has_wiki"
        homepage <: string:
            @json_tag = "homepage"
        hooks_url <: string:
            @json_tag = "hooks_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        issue_comment_url <: string:
            @json_tag = "issue_comment_url"
        issue_events_url <: string:
            @json_tag = "issue_events_url"
        issues_url <: string:
            @json_tag = "issues_url"
        keys_url <: string:
            @json_tag = "keys_url"
        labels_url <: string:
            @json_tag = "labels_url"
        language <: string:
            @json_tag = "language"
        languages_url <: string:
            @json_tag = "languages_url"
        license <: pull-request_head_repo_license:
            @json_tag = "license"
        master_branch <: string?:
            @json_tag = "master_branch"
        merges_url <: string:
            @json_tag = "merges_url"
        milestones_url <: string:
            @json_tag = "milestones_url"
        mirror_url <: string:
            @json_tag = "mirror_url"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        notifications_url <: string:
            @json_tag = "notifications_url"
        open_issues <: int:
            @json_tag = "open_issues"
        open_issues_count <: int:
            @json_tag = "open_issues_count"
        owner <: pull-request_head_repo_owner:
            @json_tag = "owner"
        permissions <: pull-request_head_repo_permissions?:
            @json_tag = "permissions"
        private <: bool:
            @json_tag = "private"
        pulls_url <: string:
            @json_tag = "pulls_url"
        pushed_at <: datetime:
            @json_tag = "pushed_at"
        releases_url <: string:
            @json_tag = "releases_url"
        size <: int:
            @json_tag = "size"
        ssh_url <: string:
            @json_tag = "ssh_url"
        stargazers_count <: int:
            @json_tag = "stargazers_count"
        stargazers_url <: string:
            @json_tag = "stargazers_url"
        statuses_url <: string:
            @json_tag = "statuses_url"
        subscribers_url <: string:
            @json_tag = "subscribers_url"
        subscription_url <: string:
            @json_tag = "subscription_url"
        svn_url <: string:
            @json_tag = "svn_url"
        tags_url <: string:
            @json_tag = "tags_url"
        teams_url <: string:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string:
            @json_tag = "trees_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        watchers <: int:
            @json_tag = "watchers"
        watchers_count <: int:
            @json_tag = "watchers_count"

    !type pull-request_head_repo_license:
        key <: string:
            @json_tag = "key"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        spdx_id <: string:
            @json_tag = "spdx_id"
        url <: string:
            @json_tag = "url"

    !type pull-request_head_repo_owner:
        avatar_url <: string:
            @json_tag = "avatar_url"
        events_url <: string:
            @json_tag = "events_url"
        followers_url <: string:
            @json_tag = "followers_url"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type pull-request_head_repo_permissions:
        admin <: bool:
            @json_tag = "admin"
        pull <: bool:
            @json_tag = "pull"
        push <: bool:
            @json_tag = "push"

    !type pull-request_head_user:
        avatar_url <: string:
            @json_tag = "avatar_url"
        events_url <: string:
            @json_tag = "events_url"
        followers_url <: string:
            @json_tag = "followers_url"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type pull-request_labels:
        color <: string?:
            @json_tag = "color"
        default <: bool?:
            @json_tag = "default"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        url <: string?:
            @json_tag = "url"

    !type rate-limit:
        limit <: int:
            @json_tag = "limit"
        remaining <: int:
            @json_tag = "remaining"
        reset <: int:
            @json_tag = "reset"

    !type rate-limit-overview:
        rate <: rate-limit:
            @json_tag = "rate"
        resources <: rate-limit-overview_resources:
            @json_tag = "resources"

    !type rate-limit-overview_resources:
        code_scanning_upload <: rate-limit?:
            @json_tag = "code_scanning_upload"
        core <: rate-limit:
            @json_tag = "core"
        graphql <: rate-limit?:
            @json_tag = "graphql"
        integration_manifest <: rate-limit?:
            @json_tag = "integration_manifest"
        search <: rate-limit:
            @json_tag = "search"
        source_import <: rate-limit?:
            @json_tag = "source_import"

    !type reaction:
        content <: string:
            @json_tag = "content"
        created_at <: datetime:
            @json_tag = "created_at"
        id <: int:
            @json_tag = "id"
        node_id <: string:
            @json_tag = "node_id"
        user <: EXTERNAL_reaction_user:
            @json_tag = "user"

    !type reaction-rollup:
        p1 <: int:
            @json_tag = "+1"
        m1 <: int:
            @json_tag = "-1"
        confused <: int:
            @json_tag = "confused"
        eyes <: int:
            @json_tag = "eyes"
        heart <: int:
            @json_tag = "heart"
        hooray <: int:
            @json_tag = "hooray"
        laugh <: int:
            @json_tag = "laugh"
        rocket <: int:
            @json_tag = "rocket"
        total_count <: int:
            @json_tag = "total_count"
        url <: string:
            @json_tag = "url"

    !type referrer-traffic:
        count <: int:
            @json_tag = "count"
        referrer <: string:
            @json_tag = "referrer"
        uniques <: int:
            @json_tag = "uniques"

    !type release:
        assets <: sequence of release-asset:
            @json_tag = "assets"
        assets_url <: string:
            @json_tag = "assets_url"
        author <: simple-user:
            @json_tag = "author"
        body <: string?:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        created_at <: datetime:
            @json_tag = "created_at"
        draft <: bool:
            @json_tag = "draft"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        prerelease <: bool:
            @json_tag = "prerelease"
        published_at <: datetime:
            @json_tag = "published_at"
        tag_name <: string:
            @json_tag = "tag_name"
        tarball_url <: string:
            @json_tag = "tarball_url"
        target_commitish <: string:
            @json_tag = "target_commitish"
        upload_url <: string:
            @json_tag = "upload_url"
        url <: string:
            @json_tag = "url"
        zipball_url <: string:
            @json_tag = "zipball_url"

    !type release-asset:
        browser_download_url <: string:
            @json_tag = "browser_download_url"
        content_type <: string:
            @json_tag = "content_type"
        created_at <: datetime:
            @json_tag = "created_at"
        download_count <: int:
            @json_tag = "download_count"
        id <: int:
            @json_tag = "id"
        label <: string:
            @json_tag = "label"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        size <: int:
            @json_tag = "size"
        state <: string:
            @json_tag = "state"
        updated_at <: datetime:
            @json_tag = "updated_at"
        uploader <: EXTERNAL_release-asset_uploader:
            @json_tag = "uploader"
        url <: string:
            @json_tag = "url"

    !type repo-search-result-item:
        allow_merge_commit <: bool?:
            @json_tag = "allow_merge_commit"
        allow_rebase_merge <: bool?:
            @json_tag = "allow_rebase_merge"
        allow_squash_merge <: bool?:
            @json_tag = "allow_squash_merge"
        archive_url <: string:
            @json_tag = "archive_url"
        archived <: bool:
            @json_tag = "archived"
        assignees_url <: string:
            @json_tag = "assignees_url"
        blobs_url <: string:
            @json_tag = "blobs_url"
        branches_url <: string:
            @json_tag = "branches_url"
        clone_url <: string:
            @json_tag = "clone_url"
        collaborators_url <: string:
            @json_tag = "collaborators_url"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        compare_url <: string:
            @json_tag = "compare_url"
        contents_url <: string:
            @json_tag = "contents_url"
        contributors_url <: string:
            @json_tag = "contributors_url"
        created_at <: datetime:
            @json_tag = "created_at"
        default_branch <: string:
            @json_tag = "default_branch"
        delete_branch_on_merge <: bool?:
            @json_tag = "delete_branch_on_merge"
        deployments_url <: string:
            @json_tag = "deployments_url"
        description <: string:
            @json_tag = "description"
        disabled <: bool:
            @json_tag = "disabled"
        downloads_url <: string:
            @json_tag = "downloads_url"
        events_url <: string:
            @json_tag = "events_url"
        fork <: bool:
            @json_tag = "fork"
        forks <: int:
            @json_tag = "forks"
        forks_count <: int:
            @json_tag = "forks_count"
        forks_url <: string:
            @json_tag = "forks_url"
        full_name <: string:
            @json_tag = "full_name"
        git_commits_url <: string:
            @json_tag = "git_commits_url"
        git_refs_url <: string:
            @json_tag = "git_refs_url"
        git_tags_url <: string:
            @json_tag = "git_tags_url"
        git_url <: string:
            @json_tag = "git_url"
        has_downloads <: bool:
            @json_tag = "has_downloads"
        has_issues <: bool:
            @json_tag = "has_issues"
        has_pages <: bool:
            @json_tag = "has_pages"
        has_projects <: bool:
            @json_tag = "has_projects"
        has_wiki <: bool:
            @json_tag = "has_wiki"
        homepage <: string:
            @json_tag = "homepage"
        hooks_url <: string:
            @json_tag = "hooks_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        issue_comment_url <: string:
            @json_tag = "issue_comment_url"
        issue_events_url <: string:
            @json_tag = "issue_events_url"
        issues_url <: string:
            @json_tag = "issues_url"
        keys_url <: string:
            @json_tag = "keys_url"
        labels_url <: string:
            @json_tag = "labels_url"
        language <: string:
            @json_tag = "language"
        languages_url <: string:
            @json_tag = "languages_url"
        license <: EXTERNAL_repo-search-result-item_license:
            @json_tag = "license"
        master_branch <: string?:
            @json_tag = "master_branch"
        merges_url <: string:
            @json_tag = "merges_url"
        milestones_url <: string:
            @json_tag = "milestones_url"
        mirror_url <: string:
            @json_tag = "mirror_url"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        notifications_url <: string:
            @json_tag = "notifications_url"
        open_issues <: int:
            @json_tag = "open_issues"
        open_issues_count <: int:
            @json_tag = "open_issues_count"
        owner <: EXTERNAL_repo-search-result-item_owner:
            @json_tag = "owner"
        permissions <: repo-search-result-item_permissions?:
            @json_tag = "permissions"
        private <: bool:
            @json_tag = "private"
        pulls_url <: string:
            @json_tag = "pulls_url"
        pushed_at <: datetime:
            @json_tag = "pushed_at"
        releases_url <: string:
            @json_tag = "releases_url"
        score <: int:
            @json_tag = "score"
        size <: int:
            @json_tag = "size"
        ssh_url <: string:
            @json_tag = "ssh_url"
        stargazers_count <: int:
            @json_tag = "stargazers_count"
        stargazers_url <: string:
            @json_tag = "stargazers_url"
        statuses_url <: string:
            @json_tag = "statuses_url"
        subscribers_url <: string:
            @json_tag = "subscribers_url"
        subscription_url <: string:
            @json_tag = "subscription_url"
        svn_url <: string:
            @json_tag = "svn_url"
        tags_url <: string:
            @json_tag = "tags_url"
        teams_url <: string:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        text_matches <: search-result-text-matches?:
            @json_tag = "text_matches"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string:
            @json_tag = "trees_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        watchers <: int:
            @json_tag = "watchers"
        watchers_count <: int:
            @json_tag = "watchers_count"

    !type repo-search-result-item_permissions:
        admin <: bool:
            @json_tag = "admin"
        pull <: bool:
            @json_tag = "pull"
        push <: bool:
            @json_tag = "push"

    !type repository:
        allow_merge_commit <: bool?:
            @json_tag = "allow_merge_commit"
        allow_rebase_merge <: bool?:
            @json_tag = "allow_rebase_merge"
        allow_squash_merge <: bool?:
            @json_tag = "allow_squash_merge"
        archive_url <: string:
            @json_tag = "archive_url"
        archived <: bool:
            @json_tag = "archived"
        assignees_url <: string:
            @json_tag = "assignees_url"
        blobs_url <: string:
            @json_tag = "blobs_url"
        branches_url <: string:
            @json_tag = "branches_url"
        clone_url <: string:
            @json_tag = "clone_url"
        collaborators_url <: string:
            @json_tag = "collaborators_url"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        compare_url <: string:
            @json_tag = "compare_url"
        contents_url <: string:
            @json_tag = "contents_url"
        contributors_url <: string:
            @json_tag = "contributors_url"
        created_at <: datetime:
            @json_tag = "created_at"
        default_branch <: string:
            @json_tag = "default_branch"
        delete_branch_on_merge <: bool?:
            @json_tag = "delete_branch_on_merge"
        deployments_url <: string:
            @json_tag = "deployments_url"
        description <: string:
            @json_tag = "description"
        disabled <: bool:
            @json_tag = "disabled"
        downloads_url <: string:
            @json_tag = "downloads_url"
        events_url <: string:
            @json_tag = "events_url"
        fork <: bool:
            @json_tag = "fork"
        forks <: int:
            @json_tag = "forks"
        forks_count <: int:
            @json_tag = "forks_count"
        forks_url <: string:
            @json_tag = "forks_url"
        full_name <: string:
            @json_tag = "full_name"
        git_commits_url <: string:
            @json_tag = "git_commits_url"
        git_refs_url <: string:
            @json_tag = "git_refs_url"
        git_tags_url <: string:
            @json_tag = "git_tags_url"
        git_url <: string:
            @json_tag = "git_url"
        has_downloads <: bool:
            @json_tag = "has_downloads"
        has_issues <: bool:
            @json_tag = "has_issues"
        has_pages <: bool:
            @json_tag = "has_pages"
        has_projects <: bool:
            @json_tag = "has_projects"
        has_wiki <: bool:
            @json_tag = "has_wiki"
        homepage <: string:
            @json_tag = "homepage"
        hooks_url <: string:
            @json_tag = "hooks_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        is_template <: bool?:
            @json_tag = "is_template"
        issue_comment_url <: string:
            @json_tag = "issue_comment_url"
        issue_events_url <: string:
            @json_tag = "issue_events_url"
        issues_url <: string:
            @json_tag = "issues_url"
        keys_url <: string:
            @json_tag = "keys_url"
        labels_url <: string:
            @json_tag = "labels_url"
        language <: string:
            @json_tag = "language"
        languages_url <: string:
            @json_tag = "languages_url"
        license <: EXTERNAL_repository_license:
            @json_tag = "license"
        master_branch <: string?:
            @json_tag = "master_branch"
        merges_url <: string:
            @json_tag = "merges_url"
        milestones_url <: string:
            @json_tag = "milestones_url"
        mirror_url <: string:
            @json_tag = "mirror_url"
        name <: string:
            @json_tag = "name"
        network_count <: int?:
            @json_tag = "network_count"
        node_id <: string:
            @json_tag = "node_id"
        notifications_url <: string:
            @json_tag = "notifications_url"
        open_issues <: int:
            @json_tag = "open_issues"
        open_issues_count <: int:
            @json_tag = "open_issues_count"
        owner <: EXTERNAL_repository_owner:
            @json_tag = "owner"
        permissions <: repository_permissions?:
            @json_tag = "permissions"
        private <: bool:
            @json_tag = "private"
        pulls_url <: string:
            @json_tag = "pulls_url"
        pushed_at <: datetime:
            @json_tag = "pushed_at"
        releases_url <: string:
            @json_tag = "releases_url"
        size <: int:
            @json_tag = "size"
        ssh_url <: string:
            @json_tag = "ssh_url"
        stargazers_count <: int:
            @json_tag = "stargazers_count"
        stargazers_url <: string:
            @json_tag = "stargazers_url"
        starred_at <: string?:
            @json_tag = "starred_at"
        statuses_url <: string:
            @json_tag = "statuses_url"
        subscribers_count <: int?:
            @json_tag = "subscribers_count"
        subscribers_url <: string:
            @json_tag = "subscribers_url"
        subscription_url <: string:
            @json_tag = "subscription_url"
        svn_url <: string:
            @json_tag = "svn_url"
        tags_url <: string:
            @json_tag = "tags_url"
        teams_url <: string:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        template_repository <: repository_template_repository?:
            @json_tag = "template_repository"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string:
            @json_tag = "trees_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        visibility <: string?:
            @json_tag = "visibility"
        watchers <: int:
            @json_tag = "watchers"
        watchers_count <: int:
            @json_tag = "watchers_count"

    !type repository-collaborator-permission:
        permission <: string:
            @json_tag = "permission"
        user <: EXTERNAL_repository-collaborator-permission_user:
            @json_tag = "user"

    !type repository-invitation:
        created_at <: datetime:
            @json_tag = "created_at"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        invitee <: EXTERNAL_repository-invitation_invitee:
            @json_tag = "invitee"
        inviter <: EXTERNAL_repository-invitation_inviter:
            @json_tag = "inviter"
        node_id <: string:
            @json_tag = "node_id"
        permissions <: string:
            @json_tag = "permissions"
        repository <: minimal-repository:
            @json_tag = "repository"
        url <: string:
            @json_tag = "url"

    !type repository-subscription:
        created_at <: datetime:
            @json_tag = "created_at"
        ignored <: bool:
            @json_tag = "ignored"
        reason <: string:
            @json_tag = "reason"
        repository_url <: string:
            @json_tag = "repository_url"
        subscribed <: bool:
            @json_tag = "subscribed"
        url <: string:
            @json_tag = "url"

    !type repository_permissions:
        admin <: bool:
            @json_tag = "admin"
        maintain <: bool?:
            @json_tag = "maintain"
        pull <: bool:
            @json_tag = "pull"
        push <: bool:
            @json_tag = "push"
        triage <: bool?:
            @json_tag = "triage"

    !type repository_template_repository:
        allow_merge_commit <: bool?:
            @json_tag = "allow_merge_commit"
        allow_rebase_merge <: bool?:
            @json_tag = "allow_rebase_merge"
        allow_squash_merge <: bool?:
            @json_tag = "allow_squash_merge"
        archive_url <: string?:
            @json_tag = "archive_url"
        archived <: bool?:
            @json_tag = "archived"
        assignees_url <: string?:
            @json_tag = "assignees_url"
        blobs_url <: string?:
            @json_tag = "blobs_url"
        branches_url <: string?:
            @json_tag = "branches_url"
        clone_url <: string?:
            @json_tag = "clone_url"
        collaborators_url <: string?:
            @json_tag = "collaborators_url"
        comments_url <: string?:
            @json_tag = "comments_url"
        commits_url <: string?:
            @json_tag = "commits_url"
        compare_url <: string?:
            @json_tag = "compare_url"
        contents_url <: string?:
            @json_tag = "contents_url"
        contributors_url <: string?:
            @json_tag = "contributors_url"
        created_at <: string?:
            @json_tag = "created_at"
        default_branch <: string?:
            @json_tag = "default_branch"
        delete_branch_on_merge <: bool?:
            @json_tag = "delete_branch_on_merge"
        deployments_url <: string?:
            @json_tag = "deployments_url"
        description <: string?:
            @json_tag = "description"
        disabled <: bool?:
            @json_tag = "disabled"
        downloads_url <: string?:
            @json_tag = "downloads_url"
        events_url <: string?:
            @json_tag = "events_url"
        fork <: bool?:
            @json_tag = "fork"
        forks_count <: int?:
            @json_tag = "forks_count"
        forks_url <: string?:
            @json_tag = "forks_url"
        full_name <: string?:
            @json_tag = "full_name"
        git_commits_url <: string?:
            @json_tag = "git_commits_url"
        git_refs_url <: string?:
            @json_tag = "git_refs_url"
        git_tags_url <: string?:
            @json_tag = "git_tags_url"
        git_url <: string?:
            @json_tag = "git_url"
        has_downloads <: bool?:
            @json_tag = "has_downloads"
        has_issues <: bool?:
            @json_tag = "has_issues"
        has_pages <: bool?:
            @json_tag = "has_pages"
        has_projects <: bool?:
            @json_tag = "has_projects"
        has_wiki <: bool?:
            @json_tag = "has_wiki"
        homepage <: string?:
            @json_tag = "homepage"
        hooks_url <: string?:
            @json_tag = "hooks_url"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        is_template <: bool?:
            @json_tag = "is_template"
        issue_comment_url <: string?:
            @json_tag = "issue_comment_url"
        issue_events_url <: string?:
            @json_tag = "issue_events_url"
        issues_url <: string?:
            @json_tag = "issues_url"
        keys_url <: string?:
            @json_tag = "keys_url"
        labels_url <: string?:
            @json_tag = "labels_url"
        language <: string?:
            @json_tag = "language"
        languages_url <: string?:
            @json_tag = "languages_url"
        merges_url <: string?:
            @json_tag = "merges_url"
        milestones_url <: string?:
            @json_tag = "milestones_url"
        mirror_url <: string?:
            @json_tag = "mirror_url"
        name <: string?:
            @json_tag = "name"
        network_count <: int?:
            @json_tag = "network_count"
        node_id <: string?:
            @json_tag = "node_id"
        notifications_url <: string?:
            @json_tag = "notifications_url"
        open_issues_count <: int?:
            @json_tag = "open_issues_count"
        owner <: repository_template_repository_owner?:
            @json_tag = "owner"
        permissions <: repository_template_repository_permissions?:
            @json_tag = "permissions"
        private <: bool?:
            @json_tag = "private"
        pulls_url <: string?:
            @json_tag = "pulls_url"
        pushed_at <: string?:
            @json_tag = "pushed_at"
        releases_url <: string?:
            @json_tag = "releases_url"
        size <: int?:
            @json_tag = "size"
        ssh_url <: string?:
            @json_tag = "ssh_url"
        stargazers_count <: int?:
            @json_tag = "stargazers_count"
        stargazers_url <: string?:
            @json_tag = "stargazers_url"
        statuses_url <: string?:
            @json_tag = "statuses_url"
        subscribers_count <: int?:
            @json_tag = "subscribers_count"
        subscribers_url <: string?:
            @json_tag = "subscribers_url"
        subscription_url <: string?:
            @json_tag = "subscription_url"
        svn_url <: string?:
            @json_tag = "svn_url"
        tags_url <: string?:
            @json_tag = "tags_url"
        teams_url <: string?:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        template_repository <: string?:
            @json_tag = "template_repository"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string?:
            @json_tag = "trees_url"
        updated_at <: string?:
            @json_tag = "updated_at"
        url <: string?:
            @json_tag = "url"
        visibility <: string?:
            @json_tag = "visibility"
        watchers_count <: int?:
            @json_tag = "watchers_count"

    !type repository_template_repository_owner:
        avatar_url <: string?:
            @json_tag = "avatar_url"
        events_url <: string?:
            @json_tag = "events_url"
        followers_url <: string?:
            @json_tag = "followers_url"
        following_url <: string?:
            @json_tag = "following_url"
        gists_url <: string?:
            @json_tag = "gists_url"
        gravatar_id <: string?:
            @json_tag = "gravatar_id"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        login <: string?:
            @json_tag = "login"
        node_id <: string?:
            @json_tag = "node_id"
        organizations_url <: string?:
            @json_tag = "organizations_url"
        received_events_url <: string?:
            @json_tag = "received_events_url"
        repos_url <: string?:
            @json_tag = "repos_url"
        site_admin <: bool?:
            @json_tag = "site_admin"
        starred_url <: string?:
            @json_tag = "starred_url"
        subscriptions_url <: string?:
            @json_tag = "subscriptions_url"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type repository_template_repository_permissions:
        admin <: bool?:
            @json_tag = "admin"
        pull <: bool?:
            @json_tag = "pull"
        push <: bool?:
            @json_tag = "push"

    !type review-comment:
        _links <: review-comment__links:
            @json_tag = "_links"
        author_association <: string:
            @json_tag = "author_association"
        body <: string:
            @json_tag = "body"
        body_html <: string?:
            @json_tag = "body_html"
        body_text <: string?:
            @json_tag = "body_text"
        commit_id <: string:
            @json_tag = "commit_id"
        created_at <: datetime:
            @json_tag = "created_at"
        diff_hunk <: string:
            @json_tag = "diff_hunk"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        in_reply_to_id <: int?:
            @json_tag = "in_reply_to_id"
        line <: int?:
            @json_tag = "line"
        node_id <: string:
            @json_tag = "node_id"
        original_commit_id <: string:
            @json_tag = "original_commit_id"
        original_line <: int?:
            @json_tag = "original_line"
        original_position <: int:
            @json_tag = "original_position"
        original_start_line <: int?:
            @json_tag = "original_start_line"
        path <: string:
            @json_tag = "path"
        position <: int:
            @json_tag = "position"
        pull_request_review_id <: int:
            @json_tag = "pull_request_review_id"
        pull_request_url <: string:
            @json_tag = "pull_request_url"
        side <: string?:
            @json_tag = "side"
        start_line <: int?:
            @json_tag = "start_line"
        start_side <: string?:
            @json_tag = "start_side"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        user <: EXTERNAL_review-comment_user:
            @json_tag = "user"

    !type review-comment__links:
        html <: link:
            @json_tag = "html"
        pull_request <: link:
            @json_tag = "pull_request"
        self <: link:
            @json_tag = "self"

    !type runner:
        busy <: bool:
            @json_tag = "busy"
        id <: int:
            @json_tag = "id"
        labels <: sequence of runner_labels:
            @json_tag = "labels"
        name <: string:
            @json_tag = "name"
        os <: string:
            @json_tag = "os"
        status <: string:
            @json_tag = "status"

    !type runner-application:
        architecture <: string?:
            @json_tag = "architecture"
        download_url <: string?:
            @json_tag = "download_url"
        filename <: string?:
            @json_tag = "filename"
        os <: string?:
            @json_tag = "os"

    !type runner-groups-enterprise:
        default <: bool?:
            @json_tag = "default"
        id <: float?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        runners_url <: string?:
            @json_tag = "runners_url"
        selected_organizations_url <: string?:
            @json_tag = "selected_organizations_url"
        visibility <: string?:
            @json_tag = "visibility"

    !type runner-groups-org:
        default <: bool?:
            @json_tag = "default"
        id <: float?:
            @json_tag = "id"
        inherited <: bool?:
            @json_tag = "inherited"
        name <: string?:
            @json_tag = "name"
        runners_url <: string?:
            @json_tag = "runners_url"
        selected_repositories_url <: string?:
            @json_tag = "selected_repositories_url"
        visibility <: string?:
            @json_tag = "visibility"

    !type runner_labels:
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        type <: string?:
            @json_tag = "type"

    !type scim-enterprise-group:
        displayName <: string?:
            @json_tag = "displayName"
        externalId <: string?:
            @json_tag = "externalId"
        id <: string?:
            @json_tag = "id"
        members <: sequence of scim-enterprise-group_members?:
            @json_tag = "members"
        meta <: scim-enterprise-group_meta?:
            @json_tag = "meta"
        schemas <: sequence of string?:
            @json_tag = "schemas"

    !type scim-enterprise-group_members:
        dollar-ref <: string?:
            @json_tag = "$ref"
        display <: string?:
            @json_tag = "display"
        value <: string?:
            @json_tag = "value"

    !type scim-enterprise-group_meta:
        created <: string?:
            @json_tag = "created"
        lastModified <: string?:
            @json_tag = "lastModified"
        location <: string?:
            @json_tag = "location"
        resourceType <: string?:
            @json_tag = "resourceType"

    !type scim-enterprise-user:
        active <: bool?:
            @json_tag = "active"
        emails <: sequence of scim-enterprise-user_emails?:
            @json_tag = "emails"
        externalId <: string?:
            @json_tag = "externalId"
        groups <: sequence of scim-enterprise-user_groups?:
            @json_tag = "groups"
        id <: string?:
            @json_tag = "id"
        meta <: scim-enterprise-user_meta?:
            @json_tag = "meta"
        name <: scim-enterprise-user_name?:
            @json_tag = "name"
        schemas <: sequence of string?:
            @json_tag = "schemas"
        userName <: string?:
            @json_tag = "userName"

    !type scim-enterprise-user_emails:
        primary <: bool?:
            @json_tag = "primary"
        type <: string?:
            @json_tag = "type"
        value <: string?:
            @json_tag = "value"

    !type scim-enterprise-user_groups:
        value <: string?:
            @json_tag = "value"

    !type scim-enterprise-user_meta:
        created <: string?:
            @json_tag = "created"
        lastModified <: string?:
            @json_tag = "lastModified"
        location <: string?:
            @json_tag = "location"
        resourceType <: string?:
            @json_tag = "resourceType"

    !type scim-enterprise-user_name:
        familyName <: string?:
            @json_tag = "familyName"
        givenName <: string?:
            @json_tag = "givenName"

    !type scim-error:
        detail <: string?:
            @json_tag = "detail"
        documentation_url <: string?:
            @json_tag = "documentation_url"
        message <: string?:
            @json_tag = "message"
        schemas <: sequence of string?:
            @json_tag = "schemas"
        scimType <: string?:
            @json_tag = "scimType"
        status <: int?:
            @json_tag = "status"

    !type scim-group-list-enterprise:
        Resources <: sequence of scim-group-list-enterprise_Resources?:
            @json_tag = "Resources"
        itemsPerPage <: float?:
            @json_tag = "itemsPerPage"
        schemas <: sequence of string?:
            @json_tag = "schemas"
        startIndex <: float?:
            @json_tag = "startIndex"
        totalResults <: float?:
            @json_tag = "totalResults"

    !type scim-group-list-enterprise_Resources:
        displayName <: string?:
            @json_tag = "displayName"
        externalId <: string?:
            @json_tag = "externalId"
        id <: string?:
            @json_tag = "id"
        members <: sequence of scim-group-list-enterprise_Resources_members?:
            @json_tag = "members"
        meta <: scim-group-list-enterprise_Resources_meta?:
            @json_tag = "meta"
        schemas <: sequence of string?:
            @json_tag = "schemas"

    !type scim-group-list-enterprise_Resources_members:
        dollar-ref <: string?:
            @json_tag = "$ref"
        display <: string?:
            @json_tag = "display"
        value <: string?:
            @json_tag = "value"

    !type scim-group-list-enterprise_Resources_meta:
        created <: string?:
            @json_tag = "created"
        lastModified <: string?:
            @json_tag = "lastModified"
        location <: string?:
            @json_tag = "location"
        resourceType <: string?:
            @json_tag = "resourceType"

    !type scim-user:
        active <: bool:
            @json_tag = "active"
        displayName <: string?:
            @json_tag = "displayName"
        emails <: sequence of scim-user_emails:
            @json_tag = "emails"
        externalId <: string:
            @json_tag = "externalId"
        groups <: sequence of scim-user_groups?:
            @json_tag = "groups"
        id <: string:
            @json_tag = "id"
        meta <: scim-user_meta:
            @json_tag = "meta"
        name <: scim-user_name:
            @json_tag = "name"
        operations <: sequence of scim-user_operations?:
            @json_tag = "operations"
        organization_id <: int?:
            @json_tag = "organization_id"
        schemas <: sequence of string:
            @json_tag = "schemas"
        userName <: string:
            @json_tag = "userName"

    !type scim-user-list:
        Resources <: sequence of scim-user:
            @json_tag = "Resources"
        itemsPerPage <: int:
            @json_tag = "itemsPerPage"
        schemas <: sequence of string:
            @json_tag = "schemas"
        startIndex <: int:
            @json_tag = "startIndex"
        totalResults <: int:
            @json_tag = "totalResults"

    !type scim-user-list-enterprise:
        Resources <: sequence of scim-user-list-enterprise_Resources?:
            @json_tag = "Resources"
        itemsPerPage <: float?:
            @json_tag = "itemsPerPage"
        schemas <: sequence of string?:
            @json_tag = "schemas"
        startIndex <: float?:
            @json_tag = "startIndex"
        totalResults <: float?:
            @json_tag = "totalResults"

    !type scim-user-list-enterprise_Resources:
        active <: bool?:
            @json_tag = "active"
        emails <: sequence of scim-user-list-enterprise_Resources_emails?:
            @json_tag = "emails"
        externalId <: string?:
            @json_tag = "externalId"
        groups <: sequence of scim-user-list-enterprise_Resources_groups?:
            @json_tag = "groups"
        id <: string?:
            @json_tag = "id"
        meta <: scim-user-list-enterprise_Resources_meta?:
            @json_tag = "meta"
        name <: scim-user-list-enterprise_Resources_name?:
            @json_tag = "name"
        schemas <: sequence of string?:
            @json_tag = "schemas"
        userName <: string?:
            @json_tag = "userName"

    !type scim-user-list-enterprise_Resources_emails:
        primary <: bool?:
            @json_tag = "primary"
        type <: string?:
            @json_tag = "type"
        value <: string?:
            @json_tag = "value"

    !type scim-user-list-enterprise_Resources_groups:
        value <: string?:
            @json_tag = "value"

    !type scim-user-list-enterprise_Resources_meta:
        created <: string?:
            @json_tag = "created"
        lastModified <: string?:
            @json_tag = "lastModified"
        location <: string?:
            @json_tag = "location"
        resourceType <: string?:
            @json_tag = "resourceType"

    !type scim-user-list-enterprise_Resources_name:
        familyName <: string?:
            @json_tag = "familyName"
        givenName <: string?:
            @json_tag = "givenName"

    !type scim-user_emails:
        primary <: bool?:
            @json_tag = "primary"
        value <: string:
            @json_tag = "value"

    !type scim-user_groups:
        display <: string?:
            @json_tag = "display"
        value <: string?:
            @json_tag = "value"

    !type scim-user_meta:
        created <: datetime?:
            @json_tag = "created"
        lastModified <: datetime?:
            @json_tag = "lastModified"
        location <: string?:
            @json_tag = "location"
        resourceType <: string?:
            @json_tag = "resourceType"

    !type scim-user_name:
        familyName <: string:
            @json_tag = "familyName"
        formatted <: string?:
            @json_tag = "formatted"
        givenName <: string:
            @json_tag = "givenName"

    !type scim-user_operations:
        op <: string:
            @json_tag = "op"
        path <: string?:
            @json_tag = "path"
        value <: scim-user_operations_value?:
            @json_tag = "value"

    !union scim-user_operations_value:
        str
        object

    !type scoped-installation:
        account <: simple-user:
            @json_tag = "account"
        permissions <: EXTERNAL_scoped-installation_permissions:
            @json_tag = "permissions"
        repositories_url <: string:
            @json_tag = "repositories_url"
        repository_selection <: string:
            @json_tag = "repository_selection"
        single_file_name <: string:
            @json_tag = "single_file_name"

    !type search-result-text-matches_obj:
        fragment <: string?:
            @json_tag = "fragment"
        matches <: sequence of search-result-text-matches_obj_search-result-text-matches_obj_matches?:
            @json_tag = "matches"
        object_type <: string?:
            @json_tag = "object_type"
        object_url <: string?:
            @json_tag = "object_url"
        property <: string?:
            @json_tag = "property"

    !type search-result-text-matches_obj_search-result-text-matches_obj_matches:
        indices <: sequence of int?:
            @json_tag = "indices"
        text <: string?:
            @json_tag = "text"

    !type selected-actions:
        github_owned_allowed <: bool?:
            @json_tag = "github_owned_allowed"
        patterns_allowed <: sequence of string?:
            @json_tag = "patterns_allowed"
        verified_allowed <: bool?:
            @json_tag = "verified_allowed"

    !type short-blob:
        sha <: string?:
            @json_tag = "sha"
        url <: string?:
            @json_tag = "url"

    !type short-branch:
        commit <: short-branch_commit:
            @json_tag = "commit"
        name <: string:
            @json_tag = "name"
        protected <: bool:
            @json_tag = "protected"
        protection <: branch-protection?:
            @json_tag = "protection"
        protection_url <: string?:
            @json_tag = "protection_url"

    !type short-branch_commit:
        sha <: string:
            @json_tag = "sha"
        url <: string:
            @json_tag = "url"

    !type simple-commit:
        author <: simple-commit_author:
            @json_tag = "author"
        committer <: simple-commit_committer:
            @json_tag = "committer"
        id <: string:
            @json_tag = "id"
        message <: string:
            @json_tag = "message"
        timestamp <: datetime:
            @json_tag = "timestamp"
        tree_id <: string:
            @json_tag = "tree_id"

    !type simple-commit-status:
        avatar_url <: string:
            @json_tag = "avatar_url"
        context <: string:
            @json_tag = "context"
        created_at <: datetime:
            @json_tag = "created_at"
        description <: string:
            @json_tag = "description"
        id <: int:
            @json_tag = "id"
        node_id <: string:
            @json_tag = "node_id"
        required <: bool?:
            @json_tag = "required"
        state <: string:
            @json_tag = "state"
        target_url <: string:
            @json_tag = "target_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type simple-commit_author:
        email <: string:
            @json_tag = "email"
        name <: string:
            @json_tag = "name"

    !type simple-commit_committer:
        email <: string:
            @json_tag = "email"
        name <: string:
            @json_tag = "name"

    !type simple-user:
        avatar_url <: string:
            @json_tag = "avatar_url"
        events_url <: string:
            @json_tag = "events_url"
        followers_url <: string:
            @json_tag = "followers_url"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        login <: string:
            @json_tag = "login"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_at <: string?:
            @json_tag = "starred_at"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        type <: string:
            @json_tag = "type"
        url <: string:
            @json_tag = "url"

    !type stargazer:
        starred_at <: datetime:
            @json_tag = "starred_at"
        user <: EXTERNAL_stargazer_user:
            @json_tag = "user"

    !type starred-repository:
        repo <: repository:
            @json_tag = "repo"
        starred_at <: datetime:
            @json_tag = "starred_at"

    !type status:
        avatar_url <: string?:
            @json_tag = "avatar_url"
        context <: string?:
            @json_tag = "context"
        created_at <: string?:
            @json_tag = "created_at"
        creator <: simple-user?:
            @json_tag = "creator"
        description <: string?:
            @json_tag = "description"
        id <: int?:
            @json_tag = "id"
        node_id <: string?:
            @json_tag = "node_id"
        state <: string?:
            @json_tag = "state"
        target_url <: string?:
            @json_tag = "target_url"
        updated_at <: string?:
            @json_tag = "updated_at"
        url <: string?:
            @json_tag = "url"

    !type status-check-policy:
        contexts <: sequence of string:
            @json_tag = "contexts"
        contexts_url <: string:
            @json_tag = "contexts_url"
        strict <: bool:
            @json_tag = "strict"
        url <: string:
            @json_tag = "url"

    !type tag:
        commit <: tag_commit:
            @json_tag = "commit"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        tarball_url <: string:
            @json_tag = "tarball_url"
        zipball_url <: string:
            @json_tag = "zipball_url"

    !type tag_commit:
        sha <: string:
            @json_tag = "sha"
        url <: string:
            @json_tag = "url"

    !type team:
        description <: string:
            @json_tag = "description"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        members_url <: string:
            @json_tag = "members_url"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        parent <: EXTERNAL_team_parent?:
            @json_tag = "parent"
        permission <: string:
            @json_tag = "permission"
        privacy <: string?:
            @json_tag = "privacy"
        repositories_url <: string:
            @json_tag = "repositories_url"
        slug <: string:
            @json_tag = "slug"
        url <: string:
            @json_tag = "url"

    !type team-discussion:
        author <: EXTERNAL_team-discussion_author:
            @json_tag = "author"
        body <: string:
            @json_tag = "body"
        body_html <: string:
            @json_tag = "body_html"
        body_version <: string:
            @json_tag = "body_version"
        comments_count <: int:
            @json_tag = "comments_count"
        comments_url <: string:
            @json_tag = "comments_url"
        created_at <: datetime:
            @json_tag = "created_at"
        html_url <: string:
            @json_tag = "html_url"
        last_edited_at <: datetime:
            @json_tag = "last_edited_at"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        pinned <: bool:
            @json_tag = "pinned"
        private <: bool:
            @json_tag = "private"
        reactions <: reaction-rollup?:
            @json_tag = "reactions"
        team_url <: string:
            @json_tag = "team_url"
        title <: string:
            @json_tag = "title"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type team-discussion-comment:
        author <: EXTERNAL_team-discussion-comment_author:
            @json_tag = "author"
        body <: string:
            @json_tag = "body"
        body_html <: string:
            @json_tag = "body_html"
        body_version <: string:
            @json_tag = "body_version"
        created_at <: datetime:
            @json_tag = "created_at"
        discussion_url <: string:
            @json_tag = "discussion_url"
        html_url <: string:
            @json_tag = "html_url"
        last_edited_at <: datetime:
            @json_tag = "last_edited_at"
        node_id <: string:
            @json_tag = "node_id"
        number <: int:
            @json_tag = "number"
        reactions <: reaction-rollup?:
            @json_tag = "reactions"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type team-full:
        created_at <: datetime:
            @json_tag = "created_at"
        description <: string:
            @json_tag = "description"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        ldap_dn <: string?:
            @json_tag = "ldap_dn"
        members_count <: int:
            @json_tag = "members_count"
        members_url <: string:
            @json_tag = "members_url"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        organization <: organization-full:
            @json_tag = "organization"
        parent <: EXTERNAL_team-full_parent?:
            @json_tag = "parent"
        permission <: string:
            @json_tag = "permission"
        privacy <: string?:
            @json_tag = "privacy"
        repos_count <: int:
            @json_tag = "repos_count"
        repositories_url <: string:
            @json_tag = "repositories_url"
        slug <: string:
            @json_tag = "slug"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type team-membership:
        role <: string:
            @json_tag = "role"
        state <: string:
            @json_tag = "state"
        url <: string:
            @json_tag = "url"

    !type team-project:
        body <: string?:
            @json_tag = "body"
        columns_url <: string?:
            @json_tag = "columns_url"
        created_at <: string?:
            @json_tag = "created_at"
        creator <: simple-user?:
            @json_tag = "creator"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        node_id <: string?:
            @json_tag = "node_id"
        number <: int?:
            @json_tag = "number"
        organization_permission <: string?:
            @json_tag = "organization_permission"
        owner_url <: string?:
            @json_tag = "owner_url"
        permissions <: team-project_permissions?:
            @json_tag = "permissions"
        private <: bool?:
            @json_tag = "private"
        state <: string?:
            @json_tag = "state"
        updated_at <: string?:
            @json_tag = "updated_at"
        url <: string?:
            @json_tag = "url"

    !type team-project_permissions:
        admin <: bool?:
            @json_tag = "admin"
        read <: bool?:
            @json_tag = "read"
        write <: bool?:
            @json_tag = "write"

    !type team-repository:
        allow_merge_commit <: bool?:
            @json_tag = "allow_merge_commit"
        allow_rebase_merge <: bool?:
            @json_tag = "allow_rebase_merge"
        allow_squash_merge <: bool?:
            @json_tag = "allow_squash_merge"
        archive_url <: string:
            @json_tag = "archive_url"
        archived <: bool:
            @json_tag = "archived"
        assignees_url <: string:
            @json_tag = "assignees_url"
        blobs_url <: string:
            @json_tag = "blobs_url"
        branches_url <: string:
            @json_tag = "branches_url"
        clone_url <: string:
            @json_tag = "clone_url"
        collaborators_url <: string:
            @json_tag = "collaborators_url"
        comments_url <: string:
            @json_tag = "comments_url"
        commits_url <: string:
            @json_tag = "commits_url"
        compare_url <: string:
            @json_tag = "compare_url"
        contents_url <: string:
            @json_tag = "contents_url"
        contributors_url <: string:
            @json_tag = "contributors_url"
        created_at <: datetime:
            @json_tag = "created_at"
        default_branch <: string:
            @json_tag = "default_branch"
        delete_branch_on_merge <: bool?:
            @json_tag = "delete_branch_on_merge"
        deployments_url <: string:
            @json_tag = "deployments_url"
        description <: string:
            @json_tag = "description"
        disabled <: bool:
            @json_tag = "disabled"
        downloads_url <: string:
            @json_tag = "downloads_url"
        events_url <: string:
            @json_tag = "events_url"
        fork <: bool:
            @json_tag = "fork"
        forks <: int:
            @json_tag = "forks"
        forks_count <: int:
            @json_tag = "forks_count"
        forks_url <: string:
            @json_tag = "forks_url"
        full_name <: string:
            @json_tag = "full_name"
        git_commits_url <: string:
            @json_tag = "git_commits_url"
        git_refs_url <: string:
            @json_tag = "git_refs_url"
        git_tags_url <: string:
            @json_tag = "git_tags_url"
        git_url <: string:
            @json_tag = "git_url"
        has_downloads <: bool:
            @json_tag = "has_downloads"
        has_issues <: bool:
            @json_tag = "has_issues"
        has_pages <: bool:
            @json_tag = "has_pages"
        has_projects <: bool:
            @json_tag = "has_projects"
        has_wiki <: bool:
            @json_tag = "has_wiki"
        homepage <: string:
            @json_tag = "homepage"
        hooks_url <: string:
            @json_tag = "hooks_url"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        is_template <: bool?:
            @json_tag = "is_template"
        issue_comment_url <: string:
            @json_tag = "issue_comment_url"
        issue_events_url <: string:
            @json_tag = "issue_events_url"
        issues_url <: string:
            @json_tag = "issues_url"
        keys_url <: string:
            @json_tag = "keys_url"
        labels_url <: string:
            @json_tag = "labels_url"
        language <: string:
            @json_tag = "language"
        languages_url <: string:
            @json_tag = "languages_url"
        license <: EXTERNAL_team-repository_license:
            @json_tag = "license"
        master_branch <: string?:
            @json_tag = "master_branch"
        merges_url <: string:
            @json_tag = "merges_url"
        milestones_url <: string:
            @json_tag = "milestones_url"
        mirror_url <: string:
            @json_tag = "mirror_url"
        name <: string:
            @json_tag = "name"
        network_count <: int?:
            @json_tag = "network_count"
        node_id <: string:
            @json_tag = "node_id"
        notifications_url <: string:
            @json_tag = "notifications_url"
        open_issues <: int:
            @json_tag = "open_issues"
        open_issues_count <: int:
            @json_tag = "open_issues_count"
        owner <: EXTERNAL_team-repository_owner:
            @json_tag = "owner"
        permissions <: team-repository_permissions?:
            @json_tag = "permissions"
        private <: bool:
            @json_tag = "private"
        pulls_url <: string:
            @json_tag = "pulls_url"
        pushed_at <: datetime:
            @json_tag = "pushed_at"
        releases_url <: string:
            @json_tag = "releases_url"
        size <: int:
            @json_tag = "size"
        ssh_url <: string:
            @json_tag = "ssh_url"
        stargazers_count <: int:
            @json_tag = "stargazers_count"
        stargazers_url <: string:
            @json_tag = "stargazers_url"
        statuses_url <: string:
            @json_tag = "statuses_url"
        subscribers_count <: int?:
            @json_tag = "subscribers_count"
        subscribers_url <: string:
            @json_tag = "subscribers_url"
        subscription_url <: string:
            @json_tag = "subscription_url"
        svn_url <: string:
            @json_tag = "svn_url"
        tags_url <: string:
            @json_tag = "tags_url"
        teams_url <: string:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        template_repository <: team-repository_template_repository?:
            @json_tag = "template_repository"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string:
            @json_tag = "trees_url"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        visibility <: string?:
            @json_tag = "visibility"
        watchers <: int:
            @json_tag = "watchers"
        watchers_count <: int:
            @json_tag = "watchers_count"

    !type team-repository_permissions:
        admin <: bool:
            @json_tag = "admin"
        maintain <: bool?:
            @json_tag = "maintain"
        pull <: bool:
            @json_tag = "pull"
        push <: bool:
            @json_tag = "push"
        triage <: bool?:
            @json_tag = "triage"

    !type team-repository_template_repository:
        allow_merge_commit <: bool?:
            @json_tag = "allow_merge_commit"
        allow_rebase_merge <: bool?:
            @json_tag = "allow_rebase_merge"
        allow_squash_merge <: bool?:
            @json_tag = "allow_squash_merge"
        archive_url <: string?:
            @json_tag = "archive_url"
        archived <: bool?:
            @json_tag = "archived"
        assignees_url <: string?:
            @json_tag = "assignees_url"
        blobs_url <: string?:
            @json_tag = "blobs_url"
        branches_url <: string?:
            @json_tag = "branches_url"
        clone_url <: string?:
            @json_tag = "clone_url"
        collaborators_url <: string?:
            @json_tag = "collaborators_url"
        comments_url <: string?:
            @json_tag = "comments_url"
        commits_url <: string?:
            @json_tag = "commits_url"
        compare_url <: string?:
            @json_tag = "compare_url"
        contents_url <: string?:
            @json_tag = "contents_url"
        contributors_url <: string?:
            @json_tag = "contributors_url"
        created_at <: string?:
            @json_tag = "created_at"
        default_branch <: string?:
            @json_tag = "default_branch"
        delete_branch_on_merge <: bool?:
            @json_tag = "delete_branch_on_merge"
        deployments_url <: string?:
            @json_tag = "deployments_url"
        description <: string?:
            @json_tag = "description"
        disabled <: bool?:
            @json_tag = "disabled"
        downloads_url <: string?:
            @json_tag = "downloads_url"
        events_url <: string?:
            @json_tag = "events_url"
        fork <: bool?:
            @json_tag = "fork"
        forks_count <: int?:
            @json_tag = "forks_count"
        forks_url <: string?:
            @json_tag = "forks_url"
        full_name <: string?:
            @json_tag = "full_name"
        git_commits_url <: string?:
            @json_tag = "git_commits_url"
        git_refs_url <: string?:
            @json_tag = "git_refs_url"
        git_tags_url <: string?:
            @json_tag = "git_tags_url"
        git_url <: string?:
            @json_tag = "git_url"
        has_downloads <: bool?:
            @json_tag = "has_downloads"
        has_issues <: bool?:
            @json_tag = "has_issues"
        has_pages <: bool?:
            @json_tag = "has_pages"
        has_projects <: bool?:
            @json_tag = "has_projects"
        has_wiki <: bool?:
            @json_tag = "has_wiki"
        homepage <: string?:
            @json_tag = "homepage"
        hooks_url <: string?:
            @json_tag = "hooks_url"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        is_template <: bool?:
            @json_tag = "is_template"
        issue_comment_url <: string?:
            @json_tag = "issue_comment_url"
        issue_events_url <: string?:
            @json_tag = "issue_events_url"
        issues_url <: string?:
            @json_tag = "issues_url"
        keys_url <: string?:
            @json_tag = "keys_url"
        labels_url <: string?:
            @json_tag = "labels_url"
        language <: string?:
            @json_tag = "language"
        languages_url <: string?:
            @json_tag = "languages_url"
        merges_url <: string?:
            @json_tag = "merges_url"
        milestones_url <: string?:
            @json_tag = "milestones_url"
        mirror_url <: string?:
            @json_tag = "mirror_url"
        name <: string?:
            @json_tag = "name"
        network_count <: int?:
            @json_tag = "network_count"
        node_id <: string?:
            @json_tag = "node_id"
        notifications_url <: string?:
            @json_tag = "notifications_url"
        open_issues_count <: int?:
            @json_tag = "open_issues_count"
        owner <: team-repository_template_repository_owner?:
            @json_tag = "owner"
        permissions <: team-repository_template_repository_permissions?:
            @json_tag = "permissions"
        private <: bool?:
            @json_tag = "private"
        pulls_url <: string?:
            @json_tag = "pulls_url"
        pushed_at <: string?:
            @json_tag = "pushed_at"
        releases_url <: string?:
            @json_tag = "releases_url"
        size <: int?:
            @json_tag = "size"
        ssh_url <: string?:
            @json_tag = "ssh_url"
        stargazers_count <: int?:
            @json_tag = "stargazers_count"
        stargazers_url <: string?:
            @json_tag = "stargazers_url"
        statuses_url <: string?:
            @json_tag = "statuses_url"
        subscribers_count <: int?:
            @json_tag = "subscribers_count"
        subscribers_url <: string?:
            @json_tag = "subscribers_url"
        subscription_url <: string?:
            @json_tag = "subscription_url"
        svn_url <: string?:
            @json_tag = "svn_url"
        tags_url <: string?:
            @json_tag = "tags_url"
        teams_url <: string?:
            @json_tag = "teams_url"
        temp_clone_token <: string?:
            @json_tag = "temp_clone_token"
        template_repository <: string?:
            @json_tag = "template_repository"
        topics <: sequence of string?:
            @json_tag = "topics"
        trees_url <: string?:
            @json_tag = "trees_url"
        updated_at <: string?:
            @json_tag = "updated_at"
        url <: string?:
            @json_tag = "url"
        visibility <: string?:
            @json_tag = "visibility"
        watchers_count <: int?:
            @json_tag = "watchers_count"

    !type team-repository_template_repository_owner:
        avatar_url <: string?:
            @json_tag = "avatar_url"
        events_url <: string?:
            @json_tag = "events_url"
        followers_url <: string?:
            @json_tag = "followers_url"
        following_url <: string?:
            @json_tag = "following_url"
        gists_url <: string?:
            @json_tag = "gists_url"
        gravatar_id <: string?:
            @json_tag = "gravatar_id"
        html_url <: string?:
            @json_tag = "html_url"
        id <: int?:
            @json_tag = "id"
        login <: string?:
            @json_tag = "login"
        node_id <: string?:
            @json_tag = "node_id"
        organizations_url <: string?:
            @json_tag = "organizations_url"
        received_events_url <: string?:
            @json_tag = "received_events_url"
        repos_url <: string?:
            @json_tag = "repos_url"
        site_admin <: bool?:
            @json_tag = "site_admin"
        starred_url <: string?:
            @json_tag = "starred_url"
        subscriptions_url <: string?:
            @json_tag = "subscriptions_url"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type team-repository_template_repository_permissions:
        admin <: bool?:
            @json_tag = "admin"
        pull <: bool?:
            @json_tag = "pull"
        push <: bool?:
            @json_tag = "push"

    !type team-simple:
        description <: string:
            @json_tag = "description"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        ldap_dn <: string?:
            @json_tag = "ldap_dn"
        members_url <: string:
            @json_tag = "members_url"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        permission <: string:
            @json_tag = "permission"
        privacy <: string?:
            @json_tag = "privacy"
        repositories_url <: string:
            @json_tag = "repositories_url"
        slug <: string:
            @json_tag = "slug"
        url <: string:
            @json_tag = "url"

    !type thread:
        id <: string?:
            @json_tag = "id"
        last_read_at <: string?:
            @json_tag = "last_read_at"
        reason <: string?:
            @json_tag = "reason"
        repository <: minimal-repository?:
            @json_tag = "repository"
        subject <: thread_subject?:
            @json_tag = "subject"
        subscription_url <: string?:
            @json_tag = "subscription_url"
        unread <: bool?:
            @json_tag = "unread"
        updated_at <: string?:
            @json_tag = "updated_at"
        url <: string?:
            @json_tag = "url"

    !type thread-subscription:
        created_at <: datetime:
            @json_tag = "created_at"
        ignored <: bool:
            @json_tag = "ignored"
        reason <: string:
            @json_tag = "reason"
        repository_url <: string?:
            @json_tag = "repository_url"
        subscribed <: bool:
            @json_tag = "subscribed"
        thread_url <: string?:
            @json_tag = "thread_url"
        url <: string:
            @json_tag = "url"

    !type thread_subject:
        latest_comment_url <: string?:
            @json_tag = "latest_comment_url"
        title <: string?:
            @json_tag = "title"
        type <: string?:
            @json_tag = "type"
        url <: string?:
            @json_tag = "url"

    !type topic:
        names <: sequence of string?:
            @json_tag = "names"

    !type topic-search-result-item:
        aliases <: sequence of topic-search-result-item_aliases?:
            @json_tag = "aliases"
        created_at <: datetime:
            @json_tag = "created_at"
        created_by <: string:
            @json_tag = "created_by"
        curated <: bool:
            @json_tag = "curated"
        description <: string:
            @json_tag = "description"
        display_name <: string:
            @json_tag = "display_name"
        featured <: bool:
            @json_tag = "featured"
        logo_url <: string?:
            @json_tag = "logo_url"
        name <: string:
            @json_tag = "name"
        related <: sequence of topic-search-result-item_related?:
            @json_tag = "related"
        released <: string:
            @json_tag = "released"
        repository_count <: int?:
            @json_tag = "repository_count"
        score <: int:
            @json_tag = "score"
        short_description <: string:
            @json_tag = "short_description"
        text_matches <: search-result-text-matches?:
            @json_tag = "text_matches"
        updated_at <: datetime:
            @json_tag = "updated_at"

    !type topic-search-result-item_aliases:
        topic_relation <: topic-search-result-item_aliases_topic_relation?:
            @json_tag = "topic_relation"

    !type topic-search-result-item_aliases_topic_relation:
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        relation_type <: string?:
            @json_tag = "relation_type"
        topic_id <: int?:
            @json_tag = "topic_id"

    !type topic-search-result-item_related:
        topic_relation <: topic-search-result-item_related_topic_relation?:
            @json_tag = "topic_relation"

    !type topic-search-result-item_related_topic_relation:
        id <: int?:
            @json_tag = "id"
        name <: string?:
            @json_tag = "name"
        relation_type <: string?:
            @json_tag = "relation_type"
        topic_id <: int?:
            @json_tag = "topic_id"

    !type traffic:
        count <: int:
            @json_tag = "count"
        timestamp <: datetime:
            @json_tag = "timestamp"
        uniques <: int:
            @json_tag = "uniques"

    !type user-marketplace-purchase:
        account <: marketplace-account:
            @json_tag = "account"
        billing_cycle <: string:
            @json_tag = "billing_cycle"
        free_trial_ends_on <: datetime:
            @json_tag = "free_trial_ends_on"
        next_billing_date <: datetime:
            @json_tag = "next_billing_date"
        on_free_trial <: bool:
            @json_tag = "on_free_trial"
        plan <: marketplace-listing-plan:
            @json_tag = "plan"
        unit_count <: int:
            @json_tag = "unit_count"
        updated_at <: datetime:
            @json_tag = "updated_at"

    !type user-search-result-item:
        avatar_url <: string:
            @json_tag = "avatar_url"
        bio <: string?:
            @json_tag = "bio"
        blog <: string?:
            @json_tag = "blog"
        company <: string?:
            @json_tag = "company"
        created_at <: datetime?:
            @json_tag = "created_at"
        email <: string?:
            @json_tag = "email"
        events_url <: string:
            @json_tag = "events_url"
        followers <: int?:
            @json_tag = "followers"
        followers_url <: string:
            @json_tag = "followers_url"
        following <: int?:
            @json_tag = "following"
        following_url <: string:
            @json_tag = "following_url"
        gists_url <: string:
            @json_tag = "gists_url"
        gravatar_id <: string:
            @json_tag = "gravatar_id"
        hireable <: bool?:
            @json_tag = "hireable"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        location <: string?:
            @json_tag = "location"
        login <: string:
            @json_tag = "login"
        name <: string?:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        organizations_url <: string:
            @json_tag = "organizations_url"
        public_gists <: int?:
            @json_tag = "public_gists"
        public_repos <: int?:
            @json_tag = "public_repos"
        received_events_url <: string:
            @json_tag = "received_events_url"
        repos_url <: string:
            @json_tag = "repos_url"
        score <: int:
            @json_tag = "score"
        site_admin <: bool:
            @json_tag = "site_admin"
        starred_url <: string:
            @json_tag = "starred_url"
        subscriptions_url <: string:
            @json_tag = "subscriptions_url"
        suspended_at <: datetime?:
            @json_tag = "suspended_at"
        text_matches <: search-result-text-matches?:
            @json_tag = "text_matches"
        type <: string:
            @json_tag = "type"
        updated_at <: datetime?:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type validation-error:
        documentation_url <: string:
            @json_tag = "documentation_url"
        errors <: sequence of validation-error_errors?:
            @json_tag = "errors"
        message <: string:
            @json_tag = "message"

    !type validation-error-simple:
        documentation_url <: string:
            @json_tag = "documentation_url"
        errors <: sequence of string?:
            @json_tag = "errors"
        message <: string:
            @json_tag = "message"

    !type validation-error_errors:
        code <: string:
            @json_tag = "code"
        field <: string?:
            @json_tag = "field"
        index <: int?:
            @json_tag = "index"
        message <: string?:
            @json_tag = "message"
        resource <: string?:
            @json_tag = "resource"
        value <: validation-error_errors_value?:
            @json_tag = "value"

    !alias integer:
        int

    !union validation-error_errors_value:
        str
        integer

    !type verification:
        payload <: string:
            @json_tag = "payload"
        reason <: string:
            @json_tag = "reason"
        signature <: string:
            @json_tag = "signature"
        verified <: bool:
            @json_tag = "verified"

    !type view-traffic:
        count <: int:
            @json_tag = "count"
        uniques <: int:
            @json_tag = "uniques"
        views <: sequence of traffic:
            @json_tag = "views"

    !type workflow:
        badge_url <: string:
            @json_tag = "badge_url"
        created_at <: datetime:
            @json_tag = "created_at"
        deleted_at <: datetime?:
            @json_tag = "deleted_at"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        name <: string:
            @json_tag = "name"
        node_id <: string:
            @json_tag = "node_id"
        path <: string:
            @json_tag = "path"
        state <: string:
            @json_tag = "state"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"

    !type workflow-run:
        artifacts_url <: string:
            @json_tag = "artifacts_url"
        cancel_url <: string:
            @json_tag = "cancel_url"
        check_suite_url <: string:
            @json_tag = "check_suite_url"
        conclusion <: string:
            @json_tag = "conclusion"
        created_at <: datetime:
            @json_tag = "created_at"
        event <: string:
            @json_tag = "event"
        head_branch <: string:
            @json_tag = "head_branch"
        head_commit <: simple-commit:
            @json_tag = "head_commit"
        head_repository <: minimal-repository:
            @json_tag = "head_repository"
        head_repository_id <: int?:
            @json_tag = "head_repository_id"
        head_sha <: string:
            @json_tag = "head_sha"
        html_url <: string:
            @json_tag = "html_url"
        id <: int:
            @json_tag = "id"
        jobs_url <: string:
            @json_tag = "jobs_url"
        logs_url <: string:
            @json_tag = "logs_url"
        node_id <: string:
            @json_tag = "node_id"
        pull_requests <: sequence of pull-request-minimal:
            @json_tag = "pull_requests"
        repository <: minimal-repository:
            @json_tag = "repository"
        rerun_url <: string:
            @json_tag = "rerun_url"
        run_number <: int:
            @json_tag = "run_number"
        status <: string:
            @json_tag = "status"
        updated_at <: datetime:
            @json_tag = "updated_at"
        url <: string:
            @json_tag = "url"
        workflow_id <: int:
            @json_tag = "workflow_id"
        workflow_url <: string:
            @json_tag = "workflow_url"

    !type workflow-run-usage:
        billable <: workflow-run-usage_billable?:
            @json_tag = "billable"
        run_duration_ms <: int?:
            @json_tag = "run_duration_ms"

    !type workflow-run-usage_billable:
        MACOS <: workflow-run-usage_billable_MACOS?:
            @json_tag = "MACOS"
        UBUNTU <: workflow-run-usage_billable_UBUNTU?:
            @json_tag = "UBUNTU"
        WINDOWS <: workflow-run-usage_billable_WINDOWS?:
            @json_tag = "WINDOWS"

    !type workflow-run-usage_billable_MACOS:
        jobs <: int?:
            @json_tag = "jobs"
        total_ms <: int?:
            @json_tag = "total_ms"

    !type workflow-run-usage_billable_UBUNTU:
        jobs <: int?:
            @json_tag = "jobs"
        total_ms <: int?:
            @json_tag = "total_ms"

    !type workflow-run-usage_billable_WINDOWS:
        jobs <: int?:
            @json_tag = "jobs"
        total_ms <: int?:
            @json_tag = "total_ms"

    !type workflow-usage:
        billable <: workflow-usage_billable?:
            @json_tag = "billable"

    !type workflow-usage_billable:
        MACOS <: workflow-usage_billable_MACOS?:
            @json_tag = "MACOS"
        UBUNTU <: workflow-usage_billable_UBUNTU?:
            @json_tag = "UBUNTU"
        WINDOWS <: workflow-usage_billable_WINDOWS?:
            @json_tag = "WINDOWS"

    !type workflow-usage_billable_MACOS:
        total_ms <: int?:
            @json_tag = "total_ms"

    !type workflow-usage_billable_UBUNTU:
        total_ms <: int?:
            @json_tag = "total_ms"

    !type workflow-usage_billable_WINDOWS:
        total_ms <: int?:
            @json_tag = "total_ms"

    !alias EXTERNAL_actions-enabled:
        str

    !alias EXTERNAL_application-grant_user:
        str

    !alias EXTERNAL_authentication-token_permissions:
        str

    !alias EXTERNAL_authorization_installation:
        str

    !alias EXTERNAL_authorization_user:
        str

    !alias EXTERNAL_base-gist_files:
        str

    !alias EXTERNAL_base-gist_forks:
        str

    !alias EXTERNAL_base-gist_history:
        str

    !alias EXTERNAL_base-gist_owner:
        str

    !alias EXTERNAL_base-gist_user:
        str

    !alias EXTERNAL_check-run_app:
        str

    !alias EXTERNAL_check-run_pull_requests:
        str

    !alias EXTERNAL_check-suite_app:
        str

    !alias code-frequency-stat:
        sequence of int

    !alias code-scanning-alert-created-at:
        datetime

    !alias code-scanning-alert-dismissed-at:
        datetime

    !alias EXTERNAL_code-scanning-alert-dismissed-by_object:
        str

    !alias code-scanning-alert-dismissed-reason:
        str

    !alias code-scanning-alert-environment:
        str

    !alias code-scanning-alert-html-url:
        str

    !alias code-scanning-alert-instances:
        sequence of code-scanning-alert-instances_obj

    !alias code-scanning-alert-number:
        int

    !alias code-scanning-alert-ref:
        str

    !alias code-scanning-alert-url:
        str

    !alias code-scanning-analysis-analysis-key:
        str

    !alias code-scanning-analysis-commit-sha:
        str

    !alias code-scanning-analysis-created-at:
        datetime

    !alias code-scanning-analysis-environment:
        str

    !alias code-scanning-analysis-ref:
        str

    !alias code-scanning-analysis-sarif-file:
        str

    !alias code-scanning-analysis-tool-name:
        str

    !alias EXTERNAL_commit-comment_user:
        str

    !alias EXTERNAL_commit-search-result-item_author:
        str

    !alias EXTERNAL_commit-search-result-item_commit_committer:
        str

    !alias EXTERNAL_commit-search-result-item_committer:
        str

    !alias EXTERNAL_commit_author:
        str

    !alias EXTERNAL_commit_commit_author:
        str

    !alias EXTERNAL_commit_commit_committer:
        str

    !alias EXTERNAL_commit_committer:
        str

    !alias EXTERNAL_community-profile_files_code_of_conduct:
        str

    !alias EXTERNAL_community-profile_files_contributing:
        str

    !alias EXTERNAL_community-profile_files_issue_template:
        str

    !alias EXTERNAL_community-profile_files_license:
        str

    !alias EXTERNAL_community-profile_files_pull_request_template:
        str

    !alias EXTERNAL_community-profile_files_readme:
        str

    !alias content-directory:
        sequence of content-directory_obj

    !alias EXTERNAL_contributor-activity_author:
        str

    !alias EXTERNAL_deployment-status_creator:
        str

    !alias EXTERNAL_deployment-status_performed_via_github_app:
        str

    !alias EXTERNAL_deployment_creator:
        str

    !alias EXTERNAL_deployment_payload:
        str

    !alias EXTERNAL_deployment_performed_via_github_app:
        str

    !alias EXTERNAL_full-repository_license:
        str

    !alias EXTERNAL_full-repository_organization:
        str

    !alias EXTERNAL_full-repository_owner:
        str

    !alias EXTERNAL_full-repository_template_repository:
        str

    !alias EXTERNAL_gist-comment_user:
        str

    !alias EXTERNAL_gist-commit_user:
        str

    !alias EXTERNAL_gist-full:
        str

    !alias EXTERNAL_gist-simple_files:
        str

    !alias EXTERNAL_gpg-key_subkeys_emails:
        str

    !alias EXTERNAL_gpg-key_subkeys_subkeys:
        str

    !alias EXTERNAL_installation_account:
        str

    !alias EXTERNAL_installation_suspended_by:
        str

    !alias EXTERNAL_integration_owner:
        str

    !alias EXTERNAL_issue-comment_performed_via_github_app:
        str

    !alias EXTERNAL_issue-comment_user:
        str

    !alias EXTERNAL_issue-event_actor:
        str

    !alias EXTERNAL_issue-event_assignee:
        str

    !alias EXTERNAL_issue-event_assigner:
        str

    !alias EXTERNAL_issue-event_requested_reviewer:
        str

    !alias EXTERNAL_issue-event_review_requester:
        str

    !alias EXTERNAL_issue-search-result-item_assignee:
        str

    !alias EXTERNAL_issue-search-result-item_milestone:
        str

    !alias EXTERNAL_issue-search-result-item_performed_via_github_app:
        str

    !alias EXTERNAL_issue-search-result-item_user:
        str

    !alias EXTERNAL_issue-simple_assignee:
        str

    !alias EXTERNAL_issue-simple_milestone:
        str

    !alias EXTERNAL_issue-simple_performed_via_github_app:
        str

    !alias EXTERNAL_issue-simple_user:
        str

    !alias EXTERNAL_issue_assignee:
        str

    !alias EXTERNAL_issue_closed_by:
        str

    !alias EXTERNAL_issue_milestone:
        str

    !alias EXTERNAL_issue_performed_via_github_app:
        str

    !alias EXTERNAL_issue_user:
        str

    !alias EXTERNAL_language:
        str

    !alias EXTERNAL_license-content_license:
        str

    !alias EXTERNAL_migration_exclude:
        str

    !alias EXTERNAL_migration_owner:
        str

    !alias EXTERNAL_milestone_creator:
        str

    !alias EXTERNAL_minimal-repository_owner:
        str

    !alias EXTERNAL_org-membership_user:
        str

    !alias EXTERNAL_page-build_pusher:
        str

    !alias EXTERNAL_project-card_creator:
        str

    !alias EXTERNAL_project_creator:
        str

    !alias EXTERNAL_pull-request-review_user:
        str

    !alias EXTERNAL_pull-request-simple_assignee:
        str

    !alias EXTERNAL_pull-request-simple_base_user:
        str

    !alias EXTERNAL_pull-request-simple_head_user:
        str

    !alias EXTERNAL_pull-request-simple_milestone:
        str

    !alias EXTERNAL_pull-request-simple_user:
        str

    !alias EXTERNAL_pull-request_assignee:
        str

    !alias EXTERNAL_pull-request_base_repo_license:
        str

    !alias EXTERNAL_pull-request_merged_by:
        str

    !alias EXTERNAL_pull-request_milestone:
        str

    !alias EXTERNAL_pull-request_user:
        str

    !alias EXTERNAL_reaction_user:
        str

    !alias EXTERNAL_release-asset_uploader:
        str

    !alias EXTERNAL_repo-search-result-item_license:
        str

    !alias EXTERNAL_repo-search-result-item_owner:
        str

    !alias EXTERNAL_repository-collaborator-permission_user:
        str

    !alias EXTERNAL_repository-invitation_invitee:
        str

    !alias EXTERNAL_repository-invitation_inviter:
        str

    !alias EXTERNAL_repository_license:
        str

    !alias EXTERNAL_repository_owner:
        str

    !alias EXTERNAL_review-comment_user:
        str

    !alias EXTERNAL_scim-user_operations_value_object:
        str

    !alias EXTERNAL_scim-user_operations_value_object:
        str

    !alias EXTERNAL_scoped-installation_permissions:
        str

    !alias search-result-text-matches:
        sequence of search-result-text-matches_obj

    !alias selected-actions-url:
        str

    !alias EXTERNAL_stargazer_user:
        str

    !alias EXTERNAL_team-discussion-comment_author:
        str

    !alias EXTERNAL_team-discussion_author:
        str

    !alias EXTERNAL_team-full_parent:
        str

    !alias EXTERNAL_team-repository_license:
        str

    !alias EXTERNAL_team-repository_owner:
        str

    !alias EXTERNAL_team_parent:
        str
